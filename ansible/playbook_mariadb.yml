- name: Mariadb Server
  gather_facts: true
  remote_user: root

  pre_tasks:
    - name: MARIADB - Set version for this playbook
      ansible.builtin.set_fact:
        PLAYBOOK_VERSION: '5.0.0'

    - name: MARIADB - Display version
      ansible.builtin.debug:
        msg:
        - "Playbook Version: {{ PLAYBOOK_VERSION }}"

  roles:
    - base
    - security
    - mariadb
