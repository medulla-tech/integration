PULSEMAIN_FQDN: '' # PULSEMAIN_FQDN must be set
LDAPADMINPASSWD: '{{ ROOT_PASSWORD }}'
LDAPSYNCHADMINPASSWD: '' # 20 character string. Must be vaulted
LDAP_DOMAIN: '{{ ansible_domain }}'
LDAP_BASEDN: 'dc={{ LDAP_DOMAIN.split(".") | join(",dc=")}}'
