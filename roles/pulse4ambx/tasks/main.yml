---
##########################################################
#                    Compute variables                   #
##########################################################

 - name: VARS - Set XMPP_DOMAIN to ansible_hostname if not on pulsemain
   set_fact:
     XMPP_DOMAIN: "{{ ansible_hostname }}"
   when:
     - INSTALL_TYPE != 'p'

 - name: VARS - Set SERVER_FQDN to ansible_fqdn
   set_fact:
     SERVER_FQDN: "{{ ansible_fqdn }}"
   when:
     - SERVER_FQDN == ''

 - name: VARS - Set IP_ADDRESS if INTERFACE defined
   set_fact:
     IP_ADDRESS: "{{ vars['ansible_'~INTERFACE].ipv4.address }}"
   when:
     - INTERFACE != ''

 - name: VARS - Set IP_ADDRESS if INTERFACE not defined
   set_fact:
     IP_ADDRESS: "{{ ansible_default_ipv4.address }}"
   when:
     - INTERFACE == ''

 - name: VARS - Set IP_REMOTE_SERVER if INTERFACE_TO_PULSE defined
   set_fact:
     IP_REMOTE_SERVER: "{{ vars['ansible_'~INTERFACE_TO_PULSE].ipv4.address }}"
   when:
     - INTERFACE_TO_PULSE != ''
     - INSTALL_TYPE != 'p'

 - name: VARS - Set IP_REMOTE_SERVER if INTERFACE_TO_PULSE not defined
   set_fact:
     IP_REMOTE_SERVER: "{{ ansible_default_ipv4.address }}"
   when:
     - INTERFACE_TO_PULSE == ''
     - INSTALL_TYPE != 'p'

 - name: VARS - Set CLUSTER_NAME to MULTISITE_ENTITY for relay
   set_fact:
     CLUSTER_NAME: "{{ MULTISITE_ENTITY }}"
   when:
     - INSTALL_TYPE != 'p'
     - MULTISITE_ENTITY != ''

 - name: VARS - Set CLUSTER_NAME to ansible_hostname if entity not defined
   set_fact:
     CLUSTER_NAME: "{{ ansible_hostname }}"
   when:
     - MULTISITE_ENTITY == ''
     - MAIN_ENTITY == ''

##########################################################
#                  Preparation checks                    #
##########################################################

 - name: PREPARATION - Make sure ssh key exists
   user:
     name: root
     generate_ssh_key: yes
     ssh_key_bits: 2048
   register: sshkey

 - name: PREPARATION - Add relay ssh public key to authorized_keys on pulsemain
   authorized_key:
     user: root
     state: present
     key: "{{ sshkey.ssh_public_key }}"
   vars:
     ansible_ssh_user: root
   delegate_to: '{{ PULSEMAIN_IP }}'
   when:
     - INSTALL_TYPE != 'p'

 - name: PREPARATION - Fetch ssh public key from pulsemain
   user:
     name: root
     generate_ssh_key: yes
     ssh_key_bits: 2048
   register: pulsemain_sshkey
   vars:
     ansible_ssh_user: root
   delegate_to: '{{ PULSEMAIN_IP }}'
   when:
     - INSTALL_TYPE != 'p'

 - name: PREPARATION - Add pulsemain ssh public key to authorized_keys on relay
   authorized_key:
     user: root
     state: present
     key: "{{ pulsemain_sshkey.ssh_public_key }}"
   when:
     - INSTALL_TYPE != 'p'

 - name: PREPARATION - Create rbash shell
   file:
     src: /bin/bash
     dest: /bin/rbash
     state: link
     force: yes

 - name: PREPARATION - Delete old siveo repo
   file:
     path: '{{ item }}'
     state: absent
   with_items:
     - /etc/yum.repos.d/siveo.repo
     - /etc/apt/sources.list.d/pulse4.list

 - name: PREPARATION - Install apt-transport-https
   apt:
     update_cache: yes
     state: latest
     pkg:
       - apt-transport-https
       - python-apt
   when:
     - ansible_distribution == 'Debian'

 - name: PREPARATION - Download repo file from {{ PULSE4REPO_URL }}
   get_url:
     url: '{{ PULSE4REPO_URL }}'
     dest: /etc/yum.repos.d/pulse4.repo
   when:
     - ansible_distribution == 'CentOS'

 - name: PREPARATION - Download list file from {{ PULSE4REPO_URL }}
   get_url:
     url: '{{ PULSE4REPO_URL }}'
     dest: /etc/apt/sources.list.d/pulse4.list
   when:
     - ansible_distribution == 'Debian'

 - name: PREPARATION - Find out URL of SIVEO public key
   command: "grep -m1 -ir 'gpgkey=' /etc/yum.repos.d/pulse4.repo"
   register: grep_output
   ignore_errors: true
   when:
     - ansible_distribution == 'CentOS'

 - name: PREPARATION - Import SIVEO public key
   rpm_key:
     state: present
     key: '{{ grep_output.stdout.split("=")[1] }}'
   when:
     - ansible_distribution == 'CentOS'

 - name: PREPARATION - Import SIVEO public key
   apt_key:
     state: present
     url: '{{ PULSE4REPOKEY_URL }}'
   when:
     - ansible_distribution == 'Debian'

 - name: PREPARATION - Clean yum repo metadata
   command: yum clean metadata
   when:
     - ansible_distribution == 'CentOS'

 - name: PREPARATION - Clean yum repo cache
   command: yum clean all
   when:
     - ansible_distribution == 'CentOS'

 - name: PREPARATION - Disable pulse2-package-server and deps
   lineinfile:
     dest:  /etc/yum.conf
     state: present
     line: exclude= pulse2-common pulse2-package-server python-pulse2-common python-mmc-core pulse2-uuid-resolver python-mmc-backuppc
   when:
     - ansible_distribution == 'CentOS'

##########################################################
#                    Install base tools                  #
##########################################################

 - name: BASE - Install tools needed for the setup
   yum:
     update_cache: yes
     state: latest
     pkg:
       - crudini
       - pwgen
       - xmlstarlet
   when:
     - ansible_distribution == 'CentOS'

 - name: BASE - Install tools needed for the setup
   apt:
     update_cache: yes
     state: latest
     pkg:
       - crudini
       - pwgen
       - xmlstarlet
   when:
     - ansible_distribution == 'Debian'

##########################################################
#                   Install pulse relay                  #
##########################################################

 - name: PULSE - Install pulse-xmpp-agent-relay
   yum:
     state: latest
     pkg:
       - pulse-xmpp-agent-relay
   when:
     - ansible_distribution == 'CentOS'

 - name: PULSE - Install pulse-xmpp-agent-relay
   apt:
     state: latest
     pkg:
       - pulse-xmpp-agent-relay
   when:
     - ansible_distribution == 'Debian'

##########################################################
#                     Install apache                     #
##########################################################

 - name: APACHE - Install Apache
   yum:
     state: latest
     pkg:
       - httpd
   when:
     - ansible_distribution == 'CentOS'

 - name: APACHE - Install Apache
   apt:
     state: latest
     pkg:
       - apache2
   when:
     - ansible_distribution == 'Debian'

 - name: APACHE - Enable and disable apache module
   apache2_module:
     state: '{{ item.state }}'
     name: '{{ item.name }}'
   with_items:
     - { name: 'proxy', state: 'present' }
     - { name: 'proxy_http', state: 'present' }
     - { name: 'proxy_wstunnel', state: 'present' }
     - { name: 'proxy_fcgi', state: 'present' }
     - { name: 'mpm_prefork', state: 'absent' }
     - { name: 'mpm_event', state: 'present' }
   when:
     - ansible_distribution == "Debian"

 - name: APACHE - Define Apache variable for CentOS
   set_fact:
     APACHE_DAEMON: "httpd"
   when:
     - ansible_distribution == 'CentOS'

 - name: APACHE - Define Apache variable for Debian
   set_fact:
     APACHE_DAEMON: "apache2"
   when:
     - ansible_distribution == 'Debian'

 - name: APACHE - Ensure /etc/systemd/system/httpd.service.d exists
   file:
     path: /etc/systemd/system/{{ APACHE_DAEMON }}.service.d
     state: directory
     mode: '0755'

 - name: APACHE - Configure PrivateTmp=false in nopt.conf
   copy:
     content: "[Service]\nPrivateTmp=false"
     dest: /etc/systemd/system/{{ APACHE_DAEMON }}.service.d/nopt.conf
     mode: '0644'

 - name: APACHE - Enable service
   systemd:
     enabled: yes
     state: restarted
     daemon_reload: yes
     name: '{{ APACHE_DAEMON }}'

 - name: APACHE - Define APACHE_CONF_PATH for CentOS
   set_fact:
     APACHE_CONF_PATH: "/etc/httpd/conf.d"
   when:
     - ansible_distribution == 'CentOS'

 - name: APACHE - Define APACHE_CONF_PATH for Debian
   set_fact:
     APACHE_CONF_PATH: "/etc/apache2/conf-available"
   when:
     - ansible_distribution == 'Debian'

##########################################################
#                     Install php-fpm                    #
##########################################################

 - name: PHP - Check if php-fpm exist
   stat:
     path: /etc/init.d/php-fpm
   register: service_status

 - name: PHP - Uninstall previous version
   yum:
     pkg:
       - php-common
       - php-mysql
     state: absent
   when:
     - ansible_distribution == 'CentOS'
     - service_status.stat.exist == false

 - name: PHP - Install php
   yum:
     update_cache: yes
     pkg:
       - php
       - php-fpm
       - php-mysqlnd
     state: latest
   when:
     - ansible_distribution == 'CentOS'

 - name: PHP - Install php
   apt:
     update_cache: yes
     pkg:
       - php-fpm
     state: latest
   when:
     - ansible_distribution == 'Debian'

 - name: PHP - Enable modules
   command: a2enconf php{{ DEB_PHP_VERSION }}-fpm
   when:
     - ansible_distribution == 'Debian'

 - name: PHP - Set variable for php.ini path
   set_fact:
     PHP_INI_PATH: "/etc/php.ini"
   when:
     - ansible_distribution == 'CentOS'

 - name: PHP - Set variable for php.ini path
   set_fact:
     PHP_INI_PATH: "/etc/php/{{ DEB_PHP_VERSION }}/fpm/php.ini"
   when:
     - ansible_distribution == 'Debian'

 - name: PHP - Update php.ini
   lineinfile:
     dest: '{{ PHP_INI_PATH }}'
     regexp: "{{ item.regex }}"
     line: "{{ item.line }}"
   with_items:
     - { regex: '^post_max_size.*$', line: 'post_max_size = 200M' }
     - { regex: '^upload_max_filesize.*$', line: 'upload_max_filesize = 200M' }
     - { regex: '^memory_limit.*$', line: 'memory_limit = 1024M' }
     - { regex: '^short_open_tag.*$', line: 'short_open_tag = On' }
     - { regex: '^date.timezone.*$', line: 'date.timezone = Europe\/Paris/' }
     - { regex: '^session.cookie_httponly.*$', line: 'session.cookie_httponly = 1' }

 - name: PHP - Enable service
   systemd:
     enabled: yes
     state: restarted
     name: php-fpm
   when:
     - ansible_distribution == 'CentOS'

 - name: PHP - Enable service
   systemd:
     enabled: yes
     state: restarted
     name: php{{ DEB_PHP_VERSION }}-fpm
   when:
     - ansible_distribution == 'Debian'

 - name: PHP - Restart apache
   systemd:
     name: '{{ APACHE_DAEMON }}'
     state: restarted

##########################################################
#                     Install mariadb                    #
##########################################################

 - name: MARIADB - Install mariadb
   yum:
     state: latest
     pkg:
       - mariadb
       - mariadb-server
       - MySQL-python
   when:
     - ansible_distribution == 'CentOS'

 - name: MARIADB - Install mariadb
   apt:
     state: latest
     pkg:
       - mariadb-client-10.3
       - mariadb-server-10.3
       - mariadb-server-core-10.3
   when:
     - ansible_distribution == 'Debian'

 - name: MARIADB - Delete /var/lib/mysql and /root/.my.cnf
   file:
     path: '{{ item }}'
     state: absent
   with_items:
     - /var/lib/mysql
     - /root/.my.cnf

 - name: MARIADB - Install initial database
   command: mysql_install_db

 - name: MARIADB - Apply permissions on /var/lib/mysql/
   file:
     path: /var/lib/mysql
     state: directory
     owner: mysql
     group: mysql
     recurse: yes

 - name: MARIADB - Apply permissions on /var/log/mariadb/
   file:
     path: /var/log/mariadb
     state: directory
     owner: mysql
     group: mysql
     recurse: yes

 - name: MARIADB - Enable service
   systemd:
     enabled: yes
     state: restarted
     name: mariadb

 - name: MARIADB - Define root password
   command: mysqladmin -u {{ DBADMINUSER }} password '{{ DBADMINPASSWD }}'

 - name: MARIADB - Secure installation
   command: 'mysql -u{{ DBADMINUSER }} -p{{ DBADMINPASSWD }} mysql -e "{{ item }}"'
   with_items:
     - "FLUSH PRIVILEGES"
     - "DELETE FROM mysql.user WHERE User='{{ DBADMINUSER }}' AND Host NOT IN ('localhost', '127.0.0.1', '::1')"
     - "DELETE FROM mysql.user WHERE User=''"
     - "DELETE FROM mysql.db WHERE Db='test' OR Db='test\_%'"
     - "FLUSH PRIVILEGES"

##########################################################
#                    Install ejabberd                    #
##########################################################

 - name: EJABBERD - Install ejabberd
   yum:
     state: latest
     pkg:
       - ejabberd
   when:
     - ansible_distribution == 'CentOS'

 - name: EJABBERD - Install ejabberd
   apt:
     state: latest
     pkg:
       - ejabberd
   when:
     - ansible_distribution == 'Debian'

 - name: EJABBERD - Enable service
   systemd:
     enabled: yes
     name: ejabberd

 - name: EJABBERD - Stop service
   systemd:
     name: ejabberd
     state: stopped

 - name: EJABBERD - Delete /var/lib/ejabberd
   file:
     path: /var/lib/ejabberd
     state: absent

 - name: EJABBERD - Create /var/lib/ejabberd
   file:
     path: /var/lib/ejabberd
     state: directory
     mode: '0755'
     owner: ejabberd
     group: ejabberd

 - name: EJABBERD - Set home folder to /var/lib/ejabberd for ejabberd user
   shell: "usermod ejabberd -d /var/lib/ejabberd"

 - name: EJABBERD - Set XMPP domain in ejabberd.yml
   command: 'sed -i "/^hosts:$/{n;s/.*/\ \ - \"{{ XMPP_DOMAIN }}\"/}" /etc/ejabberd/ejabberd.yml'

 - name: EJABBERD - Add root@{{ XMPP_DOMAIN }} as admin user in ejabberd.yml
   command: 'sed -i "/^acl:/a\ \ admin: \n \ \ \ user : \n \ \ \ \ \ - "root@{{ XMPP_DOMAIN }}"" /etc/ejabberd/ejabberd.yml'

 - name: EJABBERD - Disable starttls in ejabberd.yml
   shell: 'sed -i "s/starttls: true/starttls: false/" /etc/ejabberd/ejabberd.yml'

 - name: EJABBERD - Restart service
   systemd:
     name: ejabberd
     state: restarted

 - name: EJABBERD - Pause for 5 seconds to build ejabberd domain
   pause:
     seconds: 5

 - name: EJABBERD - Copy .erlang.cookie
   copy:
     src: /var/lib/ejabberd/.erlang.cookie
     dest: /root/.erlang.cookie
     remote_src: yes
     owner: root
     group: root
     mode: '400'

 - name: EJABBERD - register root account
   command: "ejabberdctl register root {{ XMPP_DOMAIN }} {{ ROOT_PASSWORD }}"

 - name: EJABBERD - Update ejabberd.yml
   shell: |
    sed -i 's/^##[[:space:]]registration_timeout:.*$/registration_timeout: infinity/' /etc/ejabberd/ejabberd.yml
    sed -i '/^[[:space:]]*muc_create:/{n;s/allow:.*$/allow: admin/}' /etc/ejabberd/ejabberd.yml
    sed -i '/^[[:space:]]*mod_muc:/{n;n;n;s/allow.*/allow: local/}' /etc/ejabberd/ejabberd.yml
    sed -i '/^[[:space:]]*trusted_network:/{n;s/allow:.*$/allow: all/}' /etc/ejabberd/ejabberd.yml
    sed -i '/^[[:space:]]*## mod_register:/{s/## //}' /etc/ejabberd/ejabberd.yml
    sed -i '/^[[:space:]]*##[[:space:]]*ip_access: trusted_network/{s/## //}' /etc/ejabberd/ejabberd.yml
    sed -i 's/max_stanza_size:.*$/max_stanza_size: 8388608/' /etc/ejabberd/ejabberd.yml
    sed -i 's/max_fsm_queue:.*$/max_fsm_queue: 5000/' /etc/ejabberd/ejabberd.yml
    sed -i 's/^[[:space:]]*normal:.*$/ normal: 100000000/' /etc/ejabberd/ejabberd.yml
    sed -i 's/^[[:space:]]*fast:.*$/ fast: 1000000000/' /etc/ejabberd/ejabberd.yml
    sed -i '/^[[:space:]]*##[[:space:]]*welcome_message:/{s/## //}' /etc/ejabberd/ejabberd.yml
    sed -i '/^[[:space:]]*##[[:space:]]*subject:/{s/## //;s/subject:.*$/subject: ""/}' /etc/ejabberd/ejabberd.yml
    sed -i '/^[[:space:]]*subject.*/{s/## //;s/subject:.*$/subject: ""/}' /etc/ejabberd/ejabberd.yml
    sed -i  '/^[[:space:]]*body.*/{s/## //;s/body:.*$/body: ""/}' /etc/ejabberd/ejabberd.yml
    sed -i '/body: ""/ {n;N; s/^/    ##/gm }' /etc/ejabberd/ejabberd.yml
    sed -i '/^[[:space:]]*##[[:space:]]*certfile:/{s/## //;s///}' /etc/ejabberd/ejabberd.yml
    sed -i '/^[[:space:]]*##[[:space:]]*starttls:/{s/## //;s///}' /etc/ejabberd/ejabberd.yml
    sed -i '/^[[:space:]]*#certfile:/{s/#//;s///}' /etc/ejabberd/ejabberd.yml
    sed -i '/^[[:space:]]*#starttls:/{s/#//;s///}' /etc/ejabberd/ejabberd.yml
    sed -i 's/mod_ping: {}/mod_ping:\n\ \ \ \ send_pings: true\n\ \ \ \ ping_interval: 1 min\n\ \ \ \ timeout_action: kill/' /etc/ejabberd/ejabberd.yml
   when:
     - ansible_distribution == 'CentOS'

 - name: EJABBERD - Update ejabberd.yml
   shell: |
    sed -i 's/^##[[:space:]]registration_timeout:.*$/registration_timeout: infinity/' /etc/ejabberd/ejabberd.yml
    sed -i '/^[[:space:]]*muc_create:/{n;s/allow:.*$/allow: admin/}' /etc/ejabberd/ejabberd.yml
    sed -i '/^[[:space:]]*mod_muc:/{n;n;n;s/allow.*/allow: local/}' /etc/ejabberd/ejabberd.yml
    sed -i '/^[[:space:]]*trusted_network:/{n;s/allow:.*$/allow: all/}' /etc/ejabberd/ejabberd.yml
    sed -i '/^[[:space:]]*## mod_register:/{s/## //}' /etc/ejabberd/ejabberd.yml
    sed -i '/^[[:space:]]*##[[:space:]]*ip_access: trusted_network/{s/## //}' /etc/ejabberd/ejabberd.yml
    sed -i 's/max_stanza_size:.*$/max_stanza_size: 8388608/' /etc/ejabberd/ejabberd.yml
    sed -i 's/max_fsm_queue:.*$/max_fsm_queue: 5000/' /etc/ejabberd/ejabberd.yml
    sed -i 's/^[[:space:]]*normal:.*$/ normal: 100000000/' /etc/ejabberd/ejabberd.yml
    sed -i 's/^[[:space:]]*fast:.*$/ fast: 1000000000/' /etc/ejabberd/ejabberd.yml
    sed -i '/^[[:space:]]*##[[:space:]]*welcome_message:/{s/## //}' /etc/ejabberd/ejabberd.yml
    sed -i '/^[[:space:]]*##[[:space:]]*subject:/{s/## //;s/subject:.*$/subject: ""/}' /etc/ejabberd/ejabberd.yml
    sed -i '/^[[:space:]]*##[[:space:]]*body:/{s/## //;s/body:.*$/body: ""/}' /etc/ejabberd/ejabberd.yml
    sed -i '/^[[:space:]]*##[[:space:]]*certfile:/{s/## //;s///}' /etc/ejabberd/ejabberd.yml
    sed -i '/^[[:space:]]*##[[:space:]]*starttls:/{s/## //;s///}' /etc/ejabberd/ejabberd.yml
    sed -i '/^[[:space:]]*#certfile:/{s/#//;s///}' /etc/ejabberd/ejabberd.yml
    sed -i '/^[[:space:]]*#starttls:/{s/#//;s///}' /etc/ejabberd/ejabberd.yml
    sed -i 's/mod_ping: {}/mod_ping:\n\ \ \ \ send_pings: true\n\ \ \ \ ping_interval: 1 min\n\ \ \ \ timeout_action: kill/' /etc/ejabberd/ejabberd.yml
   when:
     - ansible_distribution == 'Debian'

 - name: EJABBERD - Restart service
   systemd:
     name: ejabberd
     state: restarted

 - name: EJABBERD - Pause for 2 to 300 seconds to enable ejabberd start
   wait_for:
     port: 5222
     delay: 2

##########################################################
#                    Install guacamole                   #
##########################################################

 - name: GUACAMOLE - Install guacamole
   yum:
     pkg:
       - guacamole
       - guacd
       - libguac-client-vnc
       - libguac-client-ssh
       - libguac-client-rdp
       - guacamole-auth-mysql
       - mysql-connector-java
     state: latest
   when:
     - ansible_distribution == 'CentOS'

 - name: GUACAMOLE - Guacamole tomcat restart server true
   debconf:
     name: guacamole-tomcat
     question: guacamole-tomcat/restart-server
     vtype: boolean
     value: true
   when:
     - ansible_distribution == 'Debian'

 - name: GUACAMOLE - Install guacamole Debian 10
   apt:
     pkg:
       - guacamole-tomcat
       - libguac-client-rdp0
       - libguac-client-ssh0
       - libguac-client-vnc0
       - guacamole-auth-mysql
       - libmysql-java
     state: latest
   when:
     - ansible_distribution == 'Debian' and ansible_distribution_major_version == '10'

 - name: GUACAMOLE - Install guacamole Debian 9
   apt:
     pkg:
       - guacamole-tomcat
       - libguac-client-rdp0
       - libguac-client-ssh0
       - libguac-client-vnc0
       - guacamole-auth-jdbc
       - mysql-connector-java
     state: latest
   when:
     - ansible_distribution == 'Debian' and ansible_distribution_major_version == '9'

 - name: GUACAMOLE - Set variable of tomcat version
   set_fact:
     TOMCAT_VERSION: "tomcat"
   when:
     - ansible_distribution == 'CentOS'

 - name: GUACAMOLE - Get tomcat version
   shell: |
    cd /etc/default
    ls tomcat*
   register: versionTC
   changed_when: False
   when:
     - ansible_distribution == 'Debian'

 - name: GUACAMOLE - Set variable of tomcat version
   set_fact:
     TOMCAT_VERSION: "{{ versionTC.stdout }}"
   when:
     - ansible_distribution == 'Debian'

 - name: GUACAMOLE - Define GUACAMOLE_HOME in tomcat
   blockinfile:
     dest: /etc/default/{{ TOMCAT_VERSION }}
     create: yes
     block: |
       # Guacamole env variable
       GUACAMOLE_HOME=/etc/guacamole

 - name: GUACAMOLE - Enable tomcat
   systemd:
     enabled: yes
     state: restarted
     name: '{{ TOMCAT_VERSION }}'

 - name: GUACAMOLE - Enable guacd
   systemd:
     enabled: yes
     state: restarted
     name: guacd

 - name: GUACAMOLE - Generate Guacamole password
   command: pwgen --secure 40 1
   register: guacamole_pass

 - name: GUACAMOLE - Define GUACAMOLE_ROOT_PASSWORD variable
   set_fact:
     GUACAMOLE_ROOT_PASSWORD: "{{ guacamole_pass.stdout }}"

 - name: GUACAMOLE - Reset guacamole database
   command: 'mysql -u{{ DBADMINUSER }} -p{{ DBADMINPASSWD }} -e "{{ item }}"'
   with_items:
     - "DROP DATABASE IF EXISTS guacamole"
     - "DELETE FROM mysql.user WHERE user='guacamole'"
     - "FLUSH PRIVILEGES"
     - "CREATE DATABASE guacamole"
     - "CREATE USER 'guacamole'@'localhost' IDENTIFIED BY '{{ ROOT_PASSWORD }}'"
     - "GRANT SELECT,INSERT,UPDATE,DELETE ON guacamole.* TO 'guacamole'@'localhost'"
   no_log: True

 - name: GUACAMOLE - Set variable GUAC_SCHEMAS_PATH
   set_fact:
     GUAC_SCHEMAS_PATH: /usr/share/guacamole-auth-jdbc/mysql/schema/
   when:
     - ansible_distribution == 'CentOS'

 - name: GUACAMOLE - Set variable GUAC_SCHEMAS_PATH
   set_fact:
     GUAC_SCHEMAS_PATH: /usr/share/guacamole-auth-jdbc/mysql/schema/
   when:
     - ansible_distribution == 'Debian' and ansible_distribution_major_version == '9'

 - name: GUACAMOLE - Set variable GUAC_SCHEMAS_PATH
   set_fact:
     GUAC_SCHEMAS_PATH: /usr/share/guacamole-auth-mysql/schema/
   when:
     - ansible_distribution == 'Debian' and ansible_distribution_major_version == '10'

 - name: GUACAMOLE - Import schemas
   shell: "mysql -u{{ DBADMINUSER }} -p{{ DBADMINPASSWD }} guacamole < {{ item }}"
   with_items:
     - '{{ GUAC_SCHEMAS_PATH }}001-create-schema.sql'
     - '{{ GUAC_SCHEMAS_PATH }}002-create-admin-user.sql'

 - name: GUACAMOLE - Define guacamole root user password in database
   command: "mysql -u{{ DBADMINUSER }} -p{{ DBADMINPASSWD }} guacamole -e \"SET @salt = UNHEX(SHA2(UUID(), 256)); UPDATE guacamole_user SET username='root', password_salt=@salt, password_hash=UNHEX(SHA2(CONCAT('{{ GUACAMOLE_ROOT_PASSWORD }}', HEX(@salt)), 256)) WHERE user_id=1\""

 - name: GUACAMOLE - Create /etc/guacamole/lib/
   file:
     path: /etc/guacamole/lib/
     state: directory
     mode: '0755'
     owner: root
     group: root

 - name: GUACAMOLE - Copy mysql-connector-java.jar to /etc/guacamole/lib/
   copy:
     src: /usr/share/java/mysql-connector-java.jar
     dest: /etc/guacamole/lib/
     remote_src: yes
     owner: root
     group: root
     mode: '644'
   when:
     - ansible_distribution == 'CentOS'

 - name: GUACAMOLE - Configure guacd hostname and port
   blockinfile:
     path: /etc/guacamole/guacamole.properties
     block: |
       # Hostname and port of guacamole proxy
       guacd-hostname: localhost
       guacd-port:     4822
       # Auth provider class (authenticates user/pass combination, needed if using the provided login screen)
       auth-provider: net.sourceforge.guacamole.net.basic.BasicFileAuthenticationProvider

 - name: GUACAMOLE - Configure guacamole.properties
   lineinfile:
     dest: /etc/guacamole/guacamole.properties
     regexp: "{{ item.regex }}"
     line: "{{ item.line }}"
     state: "{{ item.state }}"
   with_items:
     - { regex: '^basic-user-mapping.*$', line: '', state: 'absent' }
     - { regex: '^noauth-config:.*$', line: '', state: 'absent' }
     - { regex: '^mysql-disallow-simultaneous-connections:.*$', line: 'mysql-disallow-simultaneous-connections: false', state: 'present' }
     - { regex: '^mysql-disallow-duplicate-connections:.*$', line: 'mysql-disallow-duplicate-connections: false', state: 'present' }
     - { regex: '^lib-directory:.*$', line: 'lib-directory: /var/lib/guacamole/classpath', state: 'present' }
     - { regex: '^mysql-hostname:.*$', line: 'mysql-hostname: localhost', state: 'present' }
     - { regex: '^mysql-port:.*$', line: 'mysql-port: 3306', state: 'present' }
     - { regex: '^mysql-database:.*$', line: 'mysql-database: guacamole', state: 'present' }
     - { regex: '^mysql-username:.*$', line: 'mysql-username: guacamole', state: 'present' }
     - { regex: '^mysql-password:.*$', line: 'mysql-password: {{ ROOT_PASSWORD }}', state: 'present' }
   no_log: True

 - name: GUACAMOLE - Change tomcat port from 8080 to 8081
   command: sed -i 's/Connector port="8080"/Connector port="8081"/' /etc/{{ TOMCAT_VERSION }}/server.xml

 - name: GUACAMOLE - Allow requests from IP_ADDRESS on pulsemain
   command: xmlstarlet ed --inplace -s "Server[@port='8005']/Service[@name='Catalina']/Engine[@name='Catalina']/Host[@name='localhost']" -t elem -n 'Valve className="org.apache.catalina.valves.RemoteAddrValve" allow="{{ IP_ADDRESS }}"' /etc/{{ TOMCAT_VERSION }}/server.xml
   when:
     - INSTALL_TYPE == "p"

 - name: GUACAMOLE - Allow requests from PULSEMAIN_IP on relay
   command: xmlstarlet ed --inplace -s "Server[@port='8005']/Service[@name='Catalina']/Engine[@name='Catalina']/Host[@name='localhost']" -t elem -n 'Valve className="org.apache.catalina.valves.RemoteAddrValve" allow="{{ PULSEMAIN_IP }}"' /etc/{{ TOMCAT_VERSION }}/server.xml
   when:
     - INSTALL_TYPE != "p"

 - name: GUACAMOLE - Restart tomcat
   systemd:
     name: '{{ TOMCAT_VERSION }}'
     state: restarted

 - name: GUACAMOLE - Configure apache for pulsemain guacamole
   copy:
     content: |
        <Location /guacamole/>
            SetEnvIf Referer "^https?://{{ SERVER_FQDN }}/" GUACAMOLE_ALLOWED
            Order Deny,Allow
            Deny from all
            Allow from env=GUACAMOLE_ALLOWED
            ProxyPass http://{{ SERVER_FQDN }}:8081/guacamole/ max=20 flushpackets=on
            ProxyPassReverse http://{{ SERVER_FQDN }}:8081/guacamole/
        </Location>
        <Location /guacamole/websocket-tunnel>
            SetEnvIf Referer "^https?://{{ SERVER_FQDN }}/" GUACAMOLE_ALLOWED
            Order Deny,Allow
            Deny from all
            Allow from env=GUACAMOLE_ALLOWED
            ProxyPass ws://{{ SERVER_FQDN }}:8081/guacamole/websocket-tunnel
            ProxyPassReverse ws://{{ SERVER_FQDN }}:8081/guacamole/websocket-tunnel
        </Location>
        SetEnvIf Request_URI "^/guacamole/tunnel" dontlog
        CustomLog  /var/log/{{ APACHE_DAEMON }}/guac.log common env=!dontlog
     dest: '{{ APACHE_CONF_PATH }}/guacamole.conf'
   when:
     - INSTALL_TYPE == "p"

 - name: GUACAMOLE - Reload apache
   systemd:
     name: '{{ APACHE_DAEMON }}'
     state: reloaded
   when:
     - INSTALL_TYPE == "p"

 - name: GUACAMOLE - Configure apache for pulsemain guacamole
   copy:
     content: |
        <Location /guacamole-{{ XMPP_DOMAIN }}/>
            SetEnvIf Referer "^https?://{{ PULSEMAIN_FQDN }}/" GUACAMOLE_ALLOWED
            Order Deny,Allow
            Deny from all
            Allow from env=GUACAMOLE_ALLOWED
            ProxyPass http://{{ XMPP_DOMAIN }}:8081/guacamole/ max=20 flushpackets=on
            ProxyPassReverse http://{{ XMPP_DOMAIN }}:8081/guacamole/
            ProxyPassReverseCookiePath /guacamole/ /guacamole-{{ XMPP_DOMAIN}}/
        </Location>
        <Location /guacamole-{{ XMPP_DOMAIN }}/websocket-tunnel>
            SetEnvIf Referer "^https?://{{ PULSEMAIN_FQDN }}/" GUACAMOLE_ALLOWED
            Order Deny,Allow
            Deny from all
            Allow from env=GUACAMOLE_ALLOWED
            ProxyPass ws://{{ XMPP_DOMAIN }}:8081/guacamole/websocket-tunnel
            ProxyPassReverse ws://{{ XMPP_DOMAIN }}:8081/guacamole/websocket-tunnel
            ProxyPassReverseCookiePath /guacamole/ /guacamole-{{XMPP_DOMAIN }}/
        </Location>
        SetEnvIf Request_URI "^/guacamole/tunnel" dontlog
        CustomLog  /var/log/{{ APACHE_DAEMON }}/guac.log common env=!dontlog
     dest: '{{ APACHE_CONF_PATH }}/guacamole-{{ XMPP_DOMAIN }}.conf'
   vars:
     ansible_ssh_user: root
   delegate_to: '{{ PULSEMAIN_IP }}'
   when:
     - INSTALL_TYPE != "p"

 - name: GUACAMOLE - Reload apache on pulsemain
   systemd:
     name: '{{ APACHE_DAEMON }}'
     state: reloaded
   vars:
     ansible_ssh_user: root
   delegate_to: '{{ PULSEMAIN_IP }}'
   when:
     - INSTALL_TYPE != "p"

##########################################################
#                     Imaging_Server                     #
##########################################################

 - name: IMAGING - Install nfs-kernel-server
   yum:
     update_cache: yes
     state: latest
     pkg:
       - nfs-utils
   when:
     - ansible_distribution == 'CentOS'

 - name: IMAGING - Install nfs-kernel-server
   apt:
     update_cache: yes
     state: latest
     pkg:
       - nfs-kernel-server
   when:
     - ansible_distribution == 'Debian'

 - name: IMAGING - Install Pulse imaging
   apt:
     state: latest
     pkg:
       - pulse2-common
       - pulse-imaging-scripts
       - pulse2-davos-client
   when:
     - ansible_distribution == 'Debian'

 - name: IMAGING - Create needed exports
   copy:
     src: '/usr/share/doc/mmc/contrib/imaging-server/exports'
     dest: '/etc/'
     remote_src: yes
     force: yes

 - name: IMAGING - Restart service nfs-kernel
   systemd:
     name: nfs-server
     state: restarted
   when:
     - ansible_distribution == 'CentOS'

 - name: IMAGING - Restart service nfs-kernel
   systemd:
     name: nfs-kernel-server
     state: restarted
   when:
     - ansible_distribution == 'Debian'

 - name: IMAGING - Install pxe-linux
   yum:
     update_cache: yes
     state: latest
     pkg:
       - syslinux-tftpboot
       - syslinux hwdata
       - hwdata
       - syslinux-efi64
   when:
     - ansible_distribution == 'CentOS'

 - name: IMAGING - Install pxe-linux
   apt:
     update_cache: yes
     state: latest
     pkg:
       - pxelinux
       - syslinux-common
       - syslinux-efi
   when:
     - ansible_distribution == 'Debian'

 - name: IMAGING - Delete old bootloader directory
   file:
     path: '{{ item }}'
     state: absent
     mode: '0755'
   with_items:
     - /var/lib/pulse2/imaging/bootloader
     - /var/lib/pulse2/imaging/bootloader-uefi32
     - /var/lib/pulse2/imaging/bootloader-uefi64
     - /home/partimag

 - name: IMAGING - Create new bootloader directory
   file:
     path: '{{ item }}'
     state: directory
   with_items:
     - /var/lib/pulse2/imaging/bootloader
     - /var/lib/pulse2/imaging/bootloader-uefi32
     - /var/lib/pulse2/imaging/bootloader-uefi64
     - /var/lib/pulse2/imaging/tools
     - /var/lib/pulse2/imaging/bootmenus
     - /home/partimag

 - name: IMAGING - Set variable path to imaging bootloader pulse
   set_fact:
     PULSE2_IM_BOOTLOADER: /var/lib/pulse2/imaging/bootloader/pxelinux.0

 - name: IMAGING - Create symlink syslinux to imaging bootloader
   file:
     path: /usr/share/syslinux/pxelinux.0
     dest: '{{ PULSE2_IM_BOOTLOADER }}'
     state: link
     force: yes
   when:
     - ansible_distribution == 'CentOS'

 - name: IMAGING - Create symlink syslinux module bios to imaging bootloader
   file:
     path: /usr/lib/syslinux/modules/bios/pxelinux.0
     dest: '{{ PULSE2_IM_BOOTLOADER }}'
     state: link
     force: yes
   when:
     - ansible_distribution == 'Debian'

 - name: IMAGING - Verify if link exist pxelinux.0
   stat:
     path: '{{ PULSE2_IM_BOOTLOADER }}'
   register: symlinkverifier

 - name: IMAGING - Create symlink syslinux to imaging bootloader
   file:
     path: /usr/share/syslinux/{{ item }}
     dest: /var/lib/pulse2/imaging/bootloader/{{ item }}
     state: link
     force: yes
   with_items:
     - libgpl.c32
     - libutil.c32
     - reboot.c32
     - ldlinux.c32
     - liblua.c32
     - vesamenu.c32
     - hdt.c32
     - libcom32.c32
     - libmenu.c32
     - poweroff.c32
   when:
     - ansible_distribution == 'CentOS'
     - symlinkverifier.stat.islnk is defined

 - name: IMAGING - Create symlink syslinux module bios to imaging bootloader
   file:
     path: /usr/lib/syslinux/modules/bios/{{ item }}
     dest: /var/lib/pulse2/imaging/bootloader/{{ item }}
     state: link
     force: yes
   with_items:
     - inventory.c32
     - libgpl.c32
     - libutil.c32
     - reboot.c32
     - ldlinux.c32
     - liblua.c32
     - vesamenu.c32
     - hdt.c32
     - libcom32.c32
     - libmenu.c32
     - poweroff.c32
   when:
     - ansible_distribution == 'Debian'
     - symlinkverifier.stat.islnk is defined

 - name: IMAGING - Make symbolic link to bootloader-uefi32-uefi64
   file:
     path: '{{ item.src }}'
     dest: '{{ item.dest }}'
     state: link
     force: yes
   with_items:
     - { src: '/usr/share/hwdata/pci.ids', dest: '/var/lib/pulse2/imaging/bootloader/pci.ids' }
     - { src: '/usr/share/syslinux/libgpl.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi32/libgpl.c32' }
     - { src: '/usr/share/syslinux/libutil.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi32/libutil.c32' }
     - { src: '/usr/share/syslinux/reboot.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi32/reboot.c32' }
     - { src: '/usr/share/syslinux/liblua.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi32/liblua.c32' }
     - { src: '/usr/share/syslinux/vesamenu.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi32/vesamenu.c32' }
     - { src: '/usr/share/syslinux/hdt.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi32/hdt.c32' }
     - { src: '/usr/share/syslinux/libcom32.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi32/libcom32.c32' }
     - { src: '/usr/share/syslinux/libmenu.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi32/libmenu.c32' }
     - { src: '/usr/share/syslinux/poweroff.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi32/poweroff.c32' }
     - { src: '/usr/share/hwdata/pci.ids', dest: '/var/lib/pulse2/imaging/bootloader-uefi32/pci.ids' }
     - { src: '/usr/share/syslinux/efi64/syslinux.efi', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/syslinux.efi'}
     - { src: '/usr/share/syslinux/efi64/libgpl.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/libgpl.c32' }
     - { src: '/usr/share/syslinux/efi64/libutil.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/libutil.c32' }
     - { src: '/usr/share/syslinux/efi64/reboot.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/reboot.c32' }
     - { src: '/usr/share/syslinux/efi64/ldlinux.e64', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/ldlinux.e64' }
     - { src: '/usr/share/syslinux/efi64/liblua.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/liblua.c32' }
     - { src: '/usr/share/syslinux/efi64/vesamenu.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/vesamenu.c32' }
     - { src: '/usr/share/syslinux/efi64/hdt.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/hdt.c32' }
     - { src: '/usr/share/syslinux/efi64/libcom32.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/libcom32.c32' }
     - { src: '/usr/share/syslinux/efi64/libmenu.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/libmenu.c32' }
     - { src: '/usr/share/syslinux/poweroff.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/poweroff.c32'}
     - { src: '/usr/share/hwdata/pci.ids', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/pci.ids'}
   when:
     - ansible_distribution == 'CentOS'
     - symlinkverifier.stat.islnk is defined

 - name: IMAGING - Make symbolic link to bootloader-uefi32-uefi64
   file:
     path: '{{ item.src }}'
     dest: '{{ item.dest }}'
     state: link
     force: yes
   with_items:
     - { src: '/usr/lib/syslinux/modules/libgpl.c32', dest: '/var/lib/pulse2/imaging/bootloader/libgpl.c32' }
     - { src: '/usr/lib/syslinux/modules/libutil.c32', dest: '/var/lib/pulse2/imaging/bootloader/libutil.c32' }
     - { src: '/usr/lib/syslinux/modules/reboot.c32', dest: '/var/lib/pulse2/imaging/bootloader/reboot.c32' }
     - { src: '/usr/lib/syslinux/modules/ldlinux.c32', dest: '/var/lib/pulse2/imaging/bootloader/ldlinux.c32' }
     - { src: '/usr/lib/syslinux/modules/liblua.c32', dest: '/var/lib/pulse2/imaging/bootloader/liblua.c32' }
     - { src: '/usr/lib/syslinux/modules/vesamenu.c32', dest: '/var/lib/pulse2/imaging/bootloader/vesamenu.c32' }
     - { src: '/usr/lib/syslinux/modules/hdt.c32', dest: '/var/lib/pulse2/imaging/bootloader/hdt.c32' }
     - { src: '/usr/lib/syslinux/modules/libcom32.c32', dest: '/var/lib/pulse2/imaging/bootloader/libcom32.c32' }
     - { src: '/usr/lib/syslinux/modules/libmenu.c32', dest: '/var/lib/pulse2/imaging/bootloader/libmenu.c32' }
     - { src: '/usr/lib/syslinux/modules/poweroff.c32', dest: '/var/lib/pulse2/imaging/bootloader/poweroff.c32' }
     - { src: '/usr/share/misc/pci.ids', dest: '/var/lib/pulse2/imaging/bootloader/pci.ids'}
     - { src: '/usr/share/misc/pci.ids', dest: '/var/lib/pulse2/imaging/bootloader-uefi32/pci.ids'}
     - { src: '/usr/lib/syslinux/modules/efi32/inventory.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi32/inventory.c32' }
     - { src: '/usr/lib/syslinux/modules/efi32/libgpl.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi32/libgpl.c32' }
     - { src: '/usr/lib/syslinux/modules/efi32/libutil.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi32/libutil.c32' }
     - { src: '/usr/lib/syslinux/modules/efi32/reboot.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi32/reboot.c32' }
     - { src: '/usr/lib/syslinux/modules/efi32/ldlinux.e32', dest: '/var/lib/pulse2/imaging/bootloader-uefi32/ldlinux.e32' }
     - { src: '/usr/lib/syslinux/modules/efi32/liblua.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi32/liblua.c32' }
     - { src: '/usr/lib/syslinux/modules/efi32/vesamenu.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi32/vesamenu.c32' }
     - { src: '/usr/lib/syslinux/modules/efi32/hdt.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi32/hdt.c32' }
     - { src: '/usr/lib/syslinux/modules/efi32/libcom32.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi32/libcom32.c32' }
     - { src: '/usr/lib/syslinux/modules/efi32/libmenu.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi32/libmenu.c32' }
     - { src: '/usr/lib/syslinux/modules/efi32/poweroff.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi32/poweroff.c32' }
     - { src: '/usr/lib/SYSLINUX.EFI/efi64/syslinux.efi', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/syslinux.efi' }
     - { src: '/usr/share/misc/pci.ids', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/pci.ids' }
     - { src: '/usr/lib/syslinux/modules/efi64/inventory.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/inventory.c32' }
     - { src: '/usr/lib/syslinux/modules/efi64/libgpl.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/libgpl.c32' }
     - { src: '/usr/lib/syslinux/modules/efi64/libutil.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/libutil.c32' }
     - { src: '/usr/lib/syslinux/modules/efi64/reboot.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/reboot.c32' }
     - { src: '/usr/lib/syslinux/modules/efi64/ldlinux.e64', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/ldlinux.e64' }
     - { src: '/usr/lib/syslinux/modules/efi64/liblua.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/liblua.c32' }
     - { src: '/usr/lib/syslinux/modules/efi64/vesamenu.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/vesamenu.c32' }
     - { src: '/usr/lib/syslinux/modules/efi64/hdt.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/hdt.c32' }
     - { src: '/usr/lib/syslinux/modules/efi64/libcom32.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/libcom32.c32' }
     - { src: '/usr/lib/syslinux/modules/efi64/libmenu.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/libmenu.c32' }
     - { src: '/usr/lib/syslinux/modules/efi64/poweroff.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/poweroff.c32' }
   when:
     - ansible_distribution == 'Debian'
     - symlinkverifier.stat.islnk is defined

 - name: IMAGING - Copy if symbolic link doesn't work to bootloader-uefi32-uefi64
   copy:
     src: '{{ item.src }}'
     dest: '{{ item.dest }}'
   with_items:
     - { src: '/usr/share/syslinux/pxelinux.0', dest: '/var/lib/pulse2/imaging/bootloader/' }
     - { src: '/usr/share/hwdata/pci.ids', dest: '/var/lib/pulse2/imaging/bootloader/' }
     - { src: '/usr/lib/syslinux/modules/libgpl.c32', dest: '/var/lib/pulse2/imaging/bootloader/' }
     - { src: '/usr/lib/syslinux/modules/libutil.c32', dest: '/var/lib/pulse2/imaging/bootloader/' }
     - { src: '/usr/lib/syslinux/modules/reboot.c32', dest: '/var/lib/pulse2/imaging/bootloader/' }
     - { src: '/usr/lib/syslinux/modules/ldlinux.c32', dest: '/var/lib/pulse2/imaging/bootloader/' }
     - { src: '/usr/lib/syslinux/modules/liblua.c32', dest: '/var/lib/pulse2/imaging/bootloader/' }
     - { src: '/usr/lib/syslinux/modules/vesamenu.c32', dest: '/var/lib/pulse2/imaging/bootloader/' }
     - { src: '/usr/lib/syslinux/modules/hdt.c32', dest: '/var/lib/pulse2/imaging/bootloader/' }
     - { src: '/usr/lib/syslinux/modules/libcom32.c32', dest: '/var/lib/pulse2/imaging/bootloader/' }
     - { src: '/usr/lib/syslinux/modules/libmenu.c32', dest: '/var/lib/pulse2/imaging/bootloader/' }
     - { src: '/usr/lib/syslinux/modules/poweroff.c32', dest: '/var/lib/pulse2/imaging/bootloader/' }
     - { src: '/usr/share/misc/pci.ids', dest: '/var/lib/pulse2/imaging/bootloader-uefi32/' }
     - { src: '/usr/lib/syslinux/modules/libgpl.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi32/' }
     - { src: '/usr/lib/syslinux/modules/libutil.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi32/' }
     - { src: '/usr/lib/syslinux/modules/reboot.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi32/' }
     - { src: '/usr/lib/syslinux/modules/liblua.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi32/' }
     - { src: '/usr/lib/syslinux/modules/vesamenu.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi32/' }
     - { src: '/usr/lib/syslinux/modules/hdt.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi32/' }
     - { src: '/usr/lib/syslinux/modules/libcom32.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi32/' }
     - { src: '/usr/lib/syslinux/modules/libmenu.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi32/' }
     - { src: '/usr/lib/syslinux/modules/poweroff.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi32/' }
     - { src: '/usr/lib/SYSLINUX.EFI/efi64/syslinux.efi', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/' }
     - { src: '/usr/share/syslinux/poweroff.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/' }
     - { src: '/usr/share/misc/pci.ids', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/' }
     - { src: '/usr/lib/syslinux/modules/efi64/libgpl.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/' }
     - { src: '/usr/lib/syslinux/modules/efi64/libutil.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/' }
     - { src: '/usr/lib/syslinux/modules/efi64/reboot.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/' }
     - { src: '/usr/lib/syslinux/modules/efi64/ldlinux.e64', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/' }
     - { src: '/usr/lib/syslinux/modules/efi64/liblua.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/' }
     - { src: '/usr/lib/syslinux/modules/efi64/vesamenu.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/' }
     - { src: '/usr/lib/syslinux/modules/efi64/hdt.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/' }
     - { src: '/usr/lib/syslinux/modules/efi64/libcom32.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/' }
     - { src: '/usr/lib/syslinux/modules/efi64/libmenu.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/' }
   when:
     - ansible_distribution == 'CentOS'
     - symlinkverifier.stat.islnk is not defined

 - name: IMAGING - Copy if symbolic link doesn't work to bootloader-uefi32-uefi64
   copy:
     src: '{{ item.src }}'
     dest: '{{ item.dest }}'
   with_items:
     - { src: '/usr/lib/PXELINUX/pxelinux.0', dest: '/var/lib/pulse2/imaging/bootloader/' }
     - { src: '/usr/share/misc/pci.ids', dest: '/var/lib/pulse2/imaging/bootloader/' }
     - { src: '/usr/lib/syslinux/modules/bios/inventory.c32', dest: '/var/lib/pulse2/imaging/bootloader/' }
     - { src: '/usr/lib/syslinux/modules/bios/libgpl.c32', dest: '/var/lib/pulse2/imaging/bootloader/' }
     - { src: '/usr/lib/syslinux/modules/bios/libutil.c32', dest: '/var/lib/pulse2/imaging/bootloader/' }
     - { src: '/usr/lib/syslinux/modules/bios/reboot.c32', dest: '/var/lib/pulse2/imaging/bootloader/' }
     - { src: '/usr/lib/syslinux/modules/bios/ldlinux.c32', dest: '/var/lib/pulse2/imaging/bootloader/' }
     - { src: '/usr/lib/syslinux/modules/bios/liblua.c32', dest: '/var/lib/pulse2/imaging/bootloader/' }
     - { src: '/usr/lib/syslinux/modules/bios/vesamenu.c32', dest: '/var/lib/pulse2/imaging/bootloader/' }
     - { src: '/usr/lib/syslinux/modules/bios/hdt.c32', dest: '/var/lib/pulse2/imaging/bootloader/' }
     - { src: '/usr/lib/syslinux/modules/bios/libcom32.c32', dest: '/var/lib/pulse2/imaging/bootloader/' }
     - { src: '/usr/lib/syslinux/modules/bios/libmenu.c32', dest: '/var/lib/pulse2/imaging/bootloader/' }
     - { src: '/usr/lib/syslinux/modules/bios/poweroff.c32', dest: '/var/lib/pulse2/imaging/bootloader/' }
     - { src: '/usr/share/misc/pci.ids', dest: '/var/lib/pulse2/imaging/bootloader-uefi32/' }
     - { src: '/usr/lib/syslinux/modules/efi32/inventory.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi32/' }
     - { src: '/usr/lib/syslinux/modules/efi32/libgpl.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi32/' }
     - { src: '/usr/lib/syslinux/modules/efi32/libutil.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi32/' }
     - { src: '/usr/lib/syslinux/modules/efi32/reboot.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi32/' }
     - { src: '/usr/lib/syslinux/modules/efi32/ldlinux.e32', dest: '/var/lib/pulse2/imaging/bootloader-uefi32/' }
     - { src: '/usr/lib/syslinux/modules/efi32/liblua.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi32/' }
     - { src: '/usr/lib/syslinux/modules/efi32/vesamenu.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi32/' }
     - { src: '/usr/lib/syslinux/modules/efi32/hdt.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi32/' }
     - { src: '/usr/lib/syslinux/modules/efi32/libcom32.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi32/' }
     - { src: '/usr/lib/syslinux/modules/efi32/libmenu.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi32/' }
     - { src: '/usr/lib/syslinux/modules/efi32/poweroff.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi32/' }
     - { src: '/usr/lib/SYSLINUX.EFI/efi64/syslinux.efi', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/' }
     - { src: '/usr/share/misc/pci.ids', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/' }
     - { src: '/usr/lib/syslinux/modules/efi64/inventory.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/' }
     - { src: '/usr/lib/syslinux/modules/efi64/libgpl.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/' }
     - { src: '/usr/lib/syslinux/modules/efi64/libutil.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/' }
     - { src: '/usr/lib/syslinux/modules/efi64/reboot.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/' }
     - { src: '/usr/lib/syslinux/modules/efi64/ldlinux.e64', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/' }
     - { src: '/usr/lib/syslinux/modules/efi64/liblua.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/' }
     - { src: '/usr/lib/syslinux/modules/efi64/vesamenu.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/' }
     - { src: '/usr/lib/syslinux/modules/efi64/hdt.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/' }
     - { src: '/usr/lib/syslinux/modules/efi64/libcom32.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/' }
     - { src: '/usr/lib/syslinux/modules/efi64/libmenu.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/' }
     - { src: '/usr/lib/syslinux/modules/efi64/poweroff.c32', dest: '/var/lib/pulse2/imaging/bootloader-uefi64/' }
   when:
     - ansible_distribution == 'Debian'
     - symlinkverifier.stat.islnk is not defined

 - name: IMAGING - Create symlink
   file:
     path: /var/lib/pulse2/imaging/bootmenus/pxelinux.cfg
     dest: '{{ item }}'
     state: link
     force: yes
   with_items:
     - /var/lib/pulse2/imaging/bootloader/pxelinux.cfg
     - /var/lib/pulse2/imaging/bootloader-uefi32/pxelinux.cfg
     - /var/lib/pulse2/imaging/bootloader-uefi64/pxelinux.cfg

 - name: IMAGING - Copy utilities.menu.in from template imaging to pulse2 imaging
   copy:
     src: /usr/share/doc/mmc/contrib/imaging/template/utilities.menu.in
     dest: /var/lib/pulse2/imaging/tools/utilities.menu
     force: no
     remote_src: yes

 - name: IMAGING - Type TFTP SERVER IP to utilities.menu
   command: 'sed -i "s/@@TFTP_SERVER_IP@@/${IP_ADDRESS}/" /var/lib/pulse2/imaging/tools/utilities.menu'

 - name: IMAGING - Install clonezilla
   yum:
     pkg:
       - clonezilla
       - pulse2-davos-client
       - udpcast
     state: latest
   when:
     - ansible_distribution == 'CentOS'

 - name: IMAGING - Install clonezilla
   apt:
     pkg:
       - clonezilla
       - pulse2-davos-client
       - udpcast
     state: latest
   when:
     - ansible_distribution == 'Debian'

 - name: IMAGING - Prevent clonezilla from being upgraded
   dpkg_selections:
     name: clonezilla
     selection: hold

 - name: IMAGING - Delete old /home/partimag
   file:
     path: /home/partimag
     state: absent

 - name: IMAGING - Create new /home/partimag
   file:
     path: /home/partimag
     state: directory

 - name: IMAGING - Link masters to /home/partimag
   file:
     path: /var/lib/pulse2/imaging/masters
     dest: /home/partimag/masters
     state: link
     force: yes

 - name: IMAGING - Udp sender extra opt
   command: 'sed -i "s/udp_sender_extra_opt_default=.*/udp_sender_extra_opt_default=\"--log \/tmp\/udp-sender.log -b 1024\"/" /etc/drbl/drbl-ocs.conf'

##########################################################
#                      Install Samba                     #
##########################################################

 - name: SAMBA - Install samba
   yum:
     pkg:
       - samba
     state: latest
   when:
     - ansible_distribution == 'CentOS'

 - name: SAMBA - Install samba
   apt:
     pkg:
       - samba
       - smbclient
     state: latest
   when:
     - ansible_distribution == 'Debian'

 - name: SAMBA - Copy file
   copy:
     src: /usr/share/doc/mmc/contrib/samba/smb.conf
     dest: /etc/samba/
     remote_src: yes

 - name: SAMBA - Samba root Password
   command: echo -ne "{{ ROOT_PASSWORD }}\n{{ ROOT_PASSWORD }}\n" | smbpasswd -a -s root

 - name: SAMBA - Users /home/drivers no home no group
   user:
     name: drivers
     shell: /bin/false

 - name: SAMBA - Samba drivers Password
   command: echo -ne "{{ DRIVERS_PASSWORD }}\n{{ DRIVERS_PASSWORD }}\n" | smbpasswd -a -s drivers

 - name: SAMBA - Set samba service name for CentOS
   set_fact:
     SAMBA_SERVICE: smb
   when:
     - ansible_distribution == 'CentOS'

 - name: SAMBA - Set samba service name for Debian
   set_fact:
     SAMBA_SERVICE: smbd
   when:
     - ansible_distribution == 'Debian'

 - name: SAMBA -  Restart service SAMBA
   systemd:
     name: '{{ SAMBA_SERVICE }}'
     state: restarted

 - name: SAMBA -  Restart service SAMBA
   systemd:
     name: '{{ SAMBA_SERVICE }}'
     enabled: yes

##########################################################
#                    Install syncthing                   #
##########################################################

 - name: SYNCTHING - Install syncthing
   yum:
     pkg:
       - syncthing
       - syncthingmanager
     state: latest
   when:
     - ansible_distribution == 'CentOS'

 - name: SYNCTHING - Install syncthing
   apt:
     pkg:
       - syncthing
       - syncthingmanager
     state: latest
   when:
     - ansible_distribution == 'Debian'

 - name: SYNCTHING - Install additional packages on pulsemain
   yum:
     pkg:
       - syncthing-tools
       - syncthing-systemd-addons
     state: latest
   when:
     - ansible_distribution == 'CentOS'
     - INSTALL_TYPE == "p"

 - name: SYNCTHING - Install additional packages on pulsemain
   apt:
     pkg:
       - syncthing_discosrv
       - syncthing-systemd-addons
     state: latest
   when:
     - ansible_distribution == 'Debian'
     - INSTALL_TYPE == "p"

 - name: SYNCTHING - Reset syncthing
   file:
     path: '{{ item }}'
     state: absent
   with_items:
     - '/var/lib/syncthing/.config'
     - '/var/lib/syncthing/Sync'
     - '/var/lib/syncthing-depl/.config'
     - '/var/lib/syncthing-depl/Sync'

 - name: SYNCTHING - Create user accounts
   user:
     name: '{{ item.name }}'
     state: present
     createhome: yes
     home: '{{ item.home }}'
     shell: /sbin/nologin
     system: yes
   with_items:
     - { name: 'syncthing', home: '/var/lib/syncthing' }
     - { name: 'syncthing-depl', home: '/var/lib/syncthing-depl' }

 - name: SYNCTHING - Create log file
   file:
     path: /var/log/syncthing.log
     mode: 0666
     state: touch

 - name: SYNCTHING - Separate syncthing logs
   lineinfile:
     dest: "/usr/lib/systemd/system/syncthing@.service"
     regexp: "{{ item.regex }}"
     line: "{{ item.line }}"
     insertafter: "{{ item.insertafter }}"
   with_items:
     - { regex: '^ExecStart=/usr/bin/syncthing .*$', line: 'ExecStart=/usr/bin/syncthing -no-browser -logflags=3 -logfile /var/log/syncthing.log', insertafter: '' }
     - { regex: '^StandardOutput=.*$', line: 'StandardOutput=null', insertafter: '^Restart=.*$' }
     - { regex: '^StandardError=.*$', line: 'StandardError=null', insertafter: 'StandardOutput=null' }
   when:
     - ansible_distribution == 'CentOS'

 - name: SYNCTHING - Separate syncthing logs
   lineinfile:
     dest: "/usr/lib/systemd/user/syncthing.service"
     regexp: "{{ item.regex }}"
     line: "{{ item.line }}"
     insertafter: "{{ item.insertafter }}"
   with_items:
     - { regex: '^ExecStart=/usr/bin/syncthing .*$', line: 'ExecStart=/usr/bin/syncthing -no-browser -logflags=3 -logfile /var/log/syncthing.log', insertafter: '' }
     - { regex: '^StandardOutput=.*$', line: 'StandardOutput=null', insertafter: '^Restart=.*$' }
     - { regex: '^StandardError=.*$', line: 'StandardError=null', insertafter: 'StandardOutput=null' }
   when:
     - ansible_distribution == 'Debian'

 - name: SYNCTHING - Setup rotation of syncthing logs
   copy:
     content: |
       /var/log/syncthing.log
       {
           missingok
           daily
           rotate 7
           notifempty
           create 0644 root root
           compress
       }
     dest: '/etc/logrotate.d/syncthing'

 - name: SYNCTHING - Enable services
   systemd:
     enabled: yes
     daemon_reload: yes
     state: restarted
     name: 'syncthing@{{ item }}.service'
   with_items:
     - syncthing
     - syncthing-depl

 - name: SYNCTHING - Pause for up to 300 seconds to enable syncthing start
   wait_for:
     path: /var/lib/syncthing/.config/syncthing/config.xml

 - name: SYNCTHING - Pause for up to 300 seconds to enable syncthing-depl start
   wait_for:
     path: /var/lib/syncthing-depl/.config/syncthing/config.xml

 - name: SYNCTHING - Create backups of syncthing configure
   copy:
     src: '/var/lib/{{ item }}/.config/syncthing/config.xml'
     dest: '/var/lib/{{ item }}/.config/syncthing/config.xml.orig'
     remote_src: yes
   with_items:
     - syncthing
     - syncthing-depl

 - name: SYNCTHING - Get local device id for syncthing
   command: syncthing -home=/var/lib/syncthing/.config/syncthing/ -device-id
   register: localdeviceid

 - name: SYNCTHING - Set LOCAL_DEVICE_ID for syncthing
   set_fact:
     LOCAL_DEVICE_ID: "{{ localdeviceid.stdout }}"

 - name: SYNCTHING - Setup local apache for syncthing
   copy:
     content: |
       ProxyPass /syncthing/ http://localhost:8384/
       <Location /syncthing/>
           ProxyPassReverse http://localhost:8384/
           Require all granted
       </Location>
     dest: '{{ APACHE_CONF_PATH }}/syncthing.conf'

 - name: SYNCTHING - Reload apache
   systemd:
     name: '{{ APACHE_DAEMON }}'
     state: reloaded

 - name: SYNCTHING - Setup pulsemain apache for relay syncthing
   copy:
     content: |
       ProxyPass /syncthing-{{ XMPP_DOMAIN }}/ http://{{ XMPP_DOMAIN }}/syncthing/
       <Location /syncthing-{{ XMPP_DOMAIN }}/>
           ProxyPassReverse http://{{ XMPP_DOMAIN }}/syncthing/
           Require all granted
       </Location>
     dest: '{{ APACHE_CONF_PATH }}/syncthing-{{ XMPP_DOMAIN }}.conf'
   vars:
     ansible_ssh_user: root
   delegate_to: '{{ PULSEMAIN_IP }}'
   when:
     - INSTALL_TYPE != 'p'

 - name: SYNCTHING - Reload apache on pulsemain
   systemd:
     name: '{{ APACHE_DAEMON }}'
     state: reloaded
   vars:
     ansible_ssh_user: root
   delegate_to: '{{ PULSEMAIN_IP }}'

 - name: SYNCTHING - Find pulsemain device id
   command: syncthing -home=/var/lib/syncthing/.config/syncthing/ -device-id
   vars:
     ansible_ssh_user: root
   delegate_to: '{{ PULSEMAIN_IP }}'
   register: resid
   when:
     - INSTALL_TYPE != 'p'

 - name: SYNCTHING - Set PULSEMAIN_DEVICE_ID
   set_fact:
     PULSEMAIN_DEVICE_ID: '{{ resid.stdout }}'
   vars:
     ansible_ssh_user: root
   delegate_to: '{{ PULSEMAIN_IP }}'
   when:
     - INSTALL_TYPE != 'p'

 - name: SYNCTHING - Set DISCOSRV_DEVICE_ID to PULSEMAIN_DEVICE_ID on relay
   set_fact:
     DISCOSRV_DEVICE_ID: '{{ PULSEMAIN_DEVICE_ID }}'
   when:
     - INSTALL_TYPE != 'p'

 - name: SYNCTHING - Stop service for editing configuration
   systemd:
     name: syncthing@{{ item }}
     state: stopped
   with_items:
     - syncthing
     - syncthing-depl

 - name: SYNCTHING - Get running processes
   shell: "ps aux | grep -v grep | grep -w syncthing | awk '{print $2}'"
   register: syncthing_pid

 - name: SYNCTHING - Wait until processes are stopped
   wait_for:
     path: "/proc/{{ item }}/status"
     state: absent
   with_items: "{{ syncthing_pid.stdout_lines }}"
   ignore_errors: yes
   register: kill_pid

 - name: SYNCTHING - Kill running processes
   command: "kill -9 {{ item }}"
   with_items: "{{ kill_pid.results | select('failed') | map(attribute='item') | list }}"

 - name: SYNCTHING - Define global announce server as pulsemain on relay syncthing
   command: 'xmlstarlet ed --inplace -u "configuration/options/globalAnnounceServer" -v "https://{{ PULSEMAIN_FQDN }}:8443/?id={{ DISCOSRV_DEVICE_ID }}" /var/lib/syncthing/.config/syncthing/config.xml'
   when:
     - INSTALL_TYPE != 'p'

 - name: SYNCTHING - Disable use of relays
   command: xmlstarlet ed --inplace -u "configuration/options/relaysEnabled" -v "false" /var/lib/syncthing/.config/syncthing/config.xml

 - name: SYNCTHING - Disable use of local announce
   command: xmlstarlet ed --inplace -u "configuration/options/localAnnounceEnabled" -v "false" /var/lib/syncthing/.config/syncthing/config.xml

 - name: SYNCTHING - Add root user
   command: xmlstarlet ed --inplace -s "configuration/gui" -t elem -n user -v root /var/lib/syncthing/.config/syncthing/config.xml

 - name: SYNCTHING - Generate Syncthing root user hash
   shell: "htpasswd -bnBC 10 '' {{ ROOT_PASSWORD }} | tr -d ':\n' | sed 's/$2y/$2a/'"
   register: syncthing_pass

 - name: SYNCTHING - Define ENCRYPTED_PASS
   set_fact:
     ENCRYPTED_PASS: "{{ syncthing_pass.stdout }}"

 - name: SYNCTHING - Set root password to ENCRYPTED_PASS
   command: xmlstarlet ed --inplace -s "configuration/gui" -t elem -n password -v {{ ENCRYPTED_PASS }} /var/lib/syncthing/.config/syncthing/config.xml

 - name: SYNCTHING - Restart service after editing configuration
   systemd:
     name: syncthing@syncthing
     state: restarted

 - name: SYNCTHING - Pause for 5 to 300 seconds to enable syncthing start
   wait_for:
     port: 22000
     delay: 5

 - name: SYNCTHING - Get api key from syncthing
   command: xmlstarlet sel -t -v "configuration/gui/apikey" /var/lib/syncthing/.config/syncthing/config.xml
   register: apikey

 - name: SYNCTHING - Set API_KEY
   set_fact:
     API_KEY: "{{ apikey.stdout }}"

 - name: SYNCTHING - Configure API_KEY in stman
   command: 'stman configure -k {{ API_KEY }}'

 - name: SYNCTHING - Define XMPP_DOMAIN for device name
   command: 'stman device edit -n {{ XMPP_DOMAIN }} {{ LOCAL_DEVICE_ID }}'

 - name: SYNCTHING - Delete relayserver device from pulsemain syncthing
   command: 'stman device remove {{ XMPP_DOMAIN }}'
   vars:
     ansible_ssh_user: root
   delegate_to: '{{ PULSEMAIN_IP }}'
   ignore_errors: yes
   when:
     - INSTALL_TYPE != 'p'

 - name: SYNCTHING - Add relayserver device to pulsemain syncthing
   command: 'stman device add --name {{ XMPP_DOMAIN }} --address tcp://{{ IP_ADDRESS }}:22000 {{ LOCAL_DEVICE_ID }}'
   vars:
     ansible_ssh_user: root
   delegate_to: '{{ PULSEMAIN_IP }}'
   when:
     - INSTALL_TYPE != 'p'

 - name: SYNCTHING - Add pulsemain device as introducer on relay syncthing
   command: 'stman device add --name pulse --address tcp://{{ PULSEMAIN_IP }}:22000 --introducer {{ PULSEMAIN_DEVICE_ID }}'
   when:
     - INSTALL_TYPE != 'p'

 - name: SYNCTHING - Stop service for editing configuration
   systemd:
     name: syncthing@{{ item }}
     state: stopped
   with_items:
     - syncthing
     - syncthing-depl
   when:
     - INSTALL_TYPE != 'p'

 - name: SYNCTHING - Get running processes
   shell: "ps aux | grep -v grep | grep -w syncthing | awk '{print $2}'"
   register: syncthing_pid
   when:
     - INSTALL_TYPE != 'p'

 - name: SYNCTHING - Wait until processes are stopped
   wait_for:
     path: "/proc/{{ item }}/status"
     state: absent
   with_items: "{{ syncthing_pid.stdout_lines }}"
   ignore_errors: yes
   register: kill_pid
   when:
     - INSTALL_TYPE != 'p'

 - name: SYNCTHING - Kill running processes
   command: "kill -9 {{ item }}"
   with_items: "{{ kill_pid.results | select('failed') | map(attribute='item') | list }}"
   when:
     - INSTALL_TYPE != 'p'

 - name: SYNCTHING - Auto-accept shares coming from pulsemain
   command: xmlstarlet ed --inplace -u "configuration/device[@id='{{ PULSEMAIN_DEVICE_ID }}']/autoAcceptFolders" -v true /var/lib/syncthing/.config/syncthing/config.xml
   when:
     - INSTALL_TYPE != 'p'

 - name: SYNCTHING - Restart service after editing configuration
   systemd:
     name: syncthing@syncthing
     state: restarted
   when:
     - INSTALL_TYPE != 'p'

 - name: SYNCTHING - Pause for 5 to 300 seconds to enable syncthing start
   wait_for:
     port: 22000
     delay: 5
   when:
     - INSTALL_TYPE != 'p'

 - name: SYNCTHING - Delete the default share
   command: 'stman folder remove "Default Folder"'

##########################################################
#               Syncthing share packages                 #
##########################################################

 - name: SHARE - Unshare folder from main pulse if already shared
   command: stman folder unshare {{ item.share_id }} {{ LOCAL_DEVICE_ID }}
   with_items:
     - { share_id: 'pulsemaster_packages', syncthing_folder: 'packages', destination: '/telediffusion/4/' }
     - { share_id: 'pulsemaster_downloads', syncthing_folder: 'downloads', destination: '/var/lib/pulse2/clients/' }
     - { share_id: 'pulsemaster_bootmenus', syncthing_folder: 'bootmenus', destination: '/var/lib/pulse2/imaging/bootmenus/' }
     - { share_id: 'pulsemaster_baseremoteagent', syncthing_folder: 'baseremoteagent', destination: '/var/lib/pulse2/xmpp_baseremoteagent/' }
   ignore_errors: yes
   vars:
     ansible_ssh_user: root
   delegate_to: '{{ PULSEMAIN_IP }}'
   when:
     - INSTALL_TYPE != 'p'

 - name: SHARE - Wait for unshare
   command: xmlstarlet sel -t -c "configuration/folder[@id='{{ item.share_id }}']/device[@id='{{ LOCAL_DEVICE_ID }}']" /var/lib/syncthing/.config/syncthing/config.xml |wc -c
   with_items:
     - { share_id: 'pulsemaster_packages', syncthing_folder: 'packages', destination: '/telediffusion/4/' }
     - { share_id: 'pulsemaster_downloads', syncthing_folder: 'downloads', destination: '/var/lib/pulse2/clients/' }
     - { share_id: 'pulsemaster_bootmenus', syncthing_folder: 'bootmenus', destination: '/var/lib/pulse2/imaging/bootmenus/' }
     - { share_id: 'pulsemaster_baseremoteagent', syncthing_folder: 'baseremoteagent', destination: '/var/lib/pulse2/xmpp_baseremoteagent/' }
   register: result
   until: result.stdout == '0'
   retries: 6
   delay: 10
   ignore_errors: yes
   when:
     - INSTALL_TYPE != 'p'

 - name: SHARE - Remove destination folders if present
   file:
     path: '{{ item.destination }}'
     state: absent
   with_items:
     - { share_id: 'pulsemaster_packages', syncthing_folder: 'packages', destination: '/telediffusion/4/' }
     - { share_id: 'pulsemaster_downloads', syncthing_folder: 'downloads', destination: '/var/lib/pulse2/clients/' }
     - { share_id: 'pulsemaster_bootmenus', syncthing_folder: 'bootmenus', destination: '/var/lib/pulse2/imaging/bootmenus/' }
     - { share_id: 'pulsemaster_baseremoteagent', syncthing_folder: 'baseremoteagent', destination: '/var/lib/pulse2/xmpp_baseremoteagent/' }
   when:
     - INSTALL_TYPE != 'p'

 - name: SHARE - Remove syncthing folders if present
   file:
     path: '/var/lib/syncthing/{{ item.syncthing_folder }}'
     state: absent
   with_items:
     - { share_id: 'pulsemaster_packages', syncthing_folder: 'packages', destination: '/telediffusion/4/' }
     - { share_id: 'pulsemaster_downloads', syncthing_folder: 'downloads', destination: '/var/lib/pulse2/clients/' }
     - { share_id: 'pulsemaster_bootmenus', syncthing_folder: 'bootmenus', destination: '/var/lib/pulse2/imaging/bootmenus/' }
     - { share_id: 'pulsemaster_baseremoteagent', syncthing_folder: 'baseremoteagent', destination: '/var/lib/pulse2/xmpp_baseremoteagent/' }
   when:
     - INSTALL_TYPE != 'p'

 - name: SHARE - Create destination folders
   file:
     path: '{{ item.destination }}'
     state: directory
   with_items:
     - { share_id: 'pulsemaster_packages', syncthing_folder: 'packages', destination: '/telediffusion/4/' }
     - { share_id: 'pulsemaster_downloads', syncthing_folder: 'downloads', destination: '/var/lib/pulse2/clients/' }
     - { share_id: 'pulsemaster_bootmenus', syncthing_folder: 'bootmenus', destination: '/var/lib/pulse2/imaging/bootmenus/' }
     - { share_id: 'pulsemaster_baseremoteagent', syncthing_folder: 'baseremoteagent', destination: '/var/lib/pulse2/xmpp_baseremoteagent/' }
   when:
     - INSTALL_TYPE != 'p'

 - name: SHARE - Create syncthing special folder
   file:
     path: '{{ item.destination }}/.stfolder/'
     state: directory
     mode: '0755'
     owner: syncthing
     group: syncthing
   with_items:
     - { share_id: 'pulsemaster_packages', syncthing_folder: 'packages', destination: '/telediffusion/4/' }
     - { share_id: 'pulsemaster_downloads', syncthing_folder: 'downloads', destination: '/var/lib/pulse2/clients/' }
     - { share_id: 'pulsemaster_bootmenus', syncthing_folder: 'bootmenus', destination: '/var/lib/pulse2/imaging/bootmenus/' }
     - { share_id: 'pulsemaster_baseremoteagent', syncthing_folder: 'baseremoteagent', destination: '/var/lib/pulse2/xmpp_baseremoteagent/' }
   when:
     - INSTALL_TYPE != 'p'

 - name: SHARE - Create stignore file for downloads on relay
   copy:
     content: "reversessh"
     dest: /var/lib/pulse2/clients/.stignore
     mode: '0644'
     owner: syncthing
     group: syncthing
   when:
     - INSTALL_TYPE != 'p'

 - name: SHARE - Delete if it's present
   file:
     state: absent
     path: /var/lib/pulse2/packages

 - name: SHARE - Create symbolic link in /var/lib/pulse2/packages
   file:
     src: /telediffusion/4
     dest: /var/lib/pulse2/packages
     owner: syncthing
     group: syncthing
     state: link

 - name: SHARE - Define ACLs on the folders with inheritance
   acl:
     path: '{{ item.destination }}'
     etype: group
     entity: syncthing
     recursive: yes
     permissions: rwx
     default: yes
     state: present
   with_items:
     - { share_id: 'pulsemaster_packages', syncthing_folder: 'packages', destination: '/telediffusion/4/' }
     - { share_id: 'pulsemaster_downloads', syncthing_folder: 'downloads', destination: '/var/lib/pulse2/clients/' }
     - { share_id: 'pulsemaster_bootmenus', syncthing_folder: 'bootmenus', destination: '/var/lib/pulse2/imaging/bootmenus/' }
     - { share_id: 'pulsemaster_baseremoteagent', syncthing_folder: 'baseremoteagent', destination: '/var/lib/pulse2/xmpp_baseremoteagent/' }
   when:
     - INSTALL_TYPE != 'p'

 - name: SHARE - Define ACLs on the folders
   acl:
     path: '{{ item.destination }}'
     etype: group
     entity: syncthing
     recursive: yes
     permissions: rwx
     default: no
     state: present
   with_items:
     - { share_id: 'pulsemaster_packages', syncthing_folder: 'packages', destination: '/telediffusion/4/' }
     - { share_id: 'pulsemaster_downloads', syncthing_folder: 'downloads', destination: '/var/lib/pulse2/clients/' }
     - { share_id: 'pulsemaster_bootmenus', syncthing_folder: 'bootmenus', destination: '/var/lib/pulse2/imaging/bootmenus/' }
     - { share_id: 'pulsemaster_baseremoteagent', syncthing_folder: 'baseremoteagent', destination: '/var/lib/pulse2/xmpp_baseremoteagent/' }
   when:
     - INSTALL_TYPE != 'p'

 - name: SHARE - Share folders to local relay from pulsemain
   command: stman folder share {{ item.share_id }} {{ LOCAL_DEVICE_ID }}
   ignore_errors: yes
   with_items:
     - { share_id: 'pulsemaster_packages', syncthing_folder: 'packages', destination: '/telediffusion/4/' }
     - { share_id: 'pulsemaster_downloads', syncthing_folder: 'downloads', destination: '/var/lib/pulse2/clients/' }
     - { share_id: 'pulsemaster_bootmenus', syncthing_folder: 'bootmenus', destination: '/var/lib/pulse2/imaging/bootmenus/' }
     - { share_id: 'pulsemaster_baseremoteagent', syncthing_folder: 'baseremoteagent', destination: '/var/lib/pulse2/xmpp_baseremoteagent/' }
   vars:
     ansible_ssh_user: root
   delegate_to: '{{ PULSEMAIN_IP }}'
   when:
     - INSTALL_TYPE != 'p'

 - name: SHARE - Wait for share
   command: xmlstarlet sel -t -c "configuration/folder[@id='{{ item.share_id }}']/device[@id='{{ LOCAL_DEVICE_ID }}']" /var/lib/syncthing/.config/syncthing/config.xml |wc -c
   register: result
   with_items:
     - { share_id: 'pulsemaster_packages', syncthing_folder: 'packages', destination: '/telediffusion/4/' }
     - { share_id: 'pulsemaster_downloads', syncthing_folder: 'downloads', destination: '/var/lib/pulse2/clients/' }
     - { share_id: 'pulsemaster_bootmenus', syncthing_folder: 'bootmenus', destination: '/var/lib/pulse2/imaging/bootmenus/' }
     - { share_id: 'pulsemaster_baseremoteagent', syncthing_folder: 'baseremoteagent', destination: '/var/lib/pulse2/xmpp_baseremoteagent/' }
   until: result.stdout == '1'
   retries: 6
   delay: 10
   when:
     - INSTALL_TYPE != 'p'

 - name: SHARE - Check if share has been added
   shell: stman folder info pulsemaster_packages| grep -i "shared with.*pulse" > /dev/null 2>&1
   register: check_share
   ignore_errors: yes

 - name: SHARE - Unshare folders from main pulse if share is not added on relay
   command: stman folder unshare {{ item.share_id }} {{ LOCAL_DEVICE_ID }}
   with_items:
     - { share_id: 'pulsemaster_packages', syncthing_folder: 'packages', destination: '/telediffusion/4/' }
     - { share_id: 'pulsemaster_downloads', syncthing_folder: 'downloads', destination: '/var/lib/pulse2/clients/' }
     - { share_id: 'pulsemaster_bootmenus', syncthing_folder: 'bootmenus', destination: '/var/lib/pulse2/imaging/bootmenus/' }
     - { share_id: 'pulsemaster_baseremoteagent', syncthing_folder: 'baseremoteagent', destination: '/var/lib/pulse2/xmpp_baseremoteagent/' }
   ignore_errors: yes
   vars:
     ansible_ssh_user: root
   delegate_to: '{{ PULSEMAIN_IP }}'
   when:
     - INSTALL_TYPE != 'p'
     - check_share.rc != 0

 - name: SHARE - Re-share folders to local relay from pulsemain
   command: stman folder share {{ item.share_id }} {{ LOCAL_DEVICE_ID }}
   with_items:
     - { share_id: 'pulsemaster_packages', syncthing_folder: 'packages', destination: '/telediffusion/4/' }
     - { share_id: 'pulsemaster_downloads', syncthing_folder: 'downloads', destination: '/var/lib/pulse2/clients/' }
     - { share_id: 'pulsemaster_bootmenus', syncthing_folder: 'bootmenus', destination: '/var/lib/pulse2/imaging/bootmenus/' }
     - { share_id: 'pulsemaster_baseremoteagent', syncthing_folder: 'baseremoteagent', destination: '/var/lib/pulse2/xmpp_baseremoteagent/' }
   ignore_errors: yes
   vars:
     ansible_ssh_user: root
   delegate_to: '{{ PULSEMAIN_IP }}'
   when:
     - INSTALL_TYPE != 'p'
     - check_share.rc != 0

 - name: SHARE - Wait for 40 seconds until shares are added
   pause:
     seconds: 40
   when:
     - INSTALL_TYPE != 'p'
     - check_share.rc != 0

 - name: SYNCTHING - Stop service for editing configuration
   systemd:
     name: syncthing@{{ item }}
     state: stopped
   with_items:
     - syncthing
     - syncthing-depl
   when:
     - INSTALL_TYPE != 'p'

 - name: SYNCTHING - Get running processes
   shell: "ps aux | grep -v grep | grep -w syncthing | awk '{print $2}'"
   register: syncthing_pid
   when:
     - INSTALL_TYPE != 'p'

 - name: SYNCTHING - Wait until processes are stopped
   wait_for:
     path: "/proc/{{ item }}/status"
     state: absent
   with_items: "{{ syncthing_pid.stdout_lines }}"
   ignore_errors: yes
   register: kill_pid
   when:
     - INSTALL_TYPE != 'p'

 - name: SYNCTHING - Kill running processes
   command: "kill -9 {{ item }}"
   with_items: "{{ kill_pid.results | select('failed') | map(attribute='item') | list }}"
   when:
     - INSTALL_TYPE != 'p'

 - name: SHARE - Remove syncthing folders from syncthing home
   file:
     path: '/var/lib/syncthing/{{ item.syncthing_folder }}'
     state: absent
   with_items:
     - { share_id: 'pulsemaster_packages', syncthing_folder: 'packages', destination: '/telediffusion/4/' }
     - { share_id: 'pulsemaster_downloads', syncthing_folder: 'downloads', destination: '/var/lib/pulse2/clients/' }
     - { share_id: 'pulsemaster_bootmenus', syncthing_folder: 'bootmenus', destination: '/var/lib/pulse2/imaging/bootmenus/' }
     - { share_id: 'pulsemaster_baseremoteagent', syncthing_folder: 'baseremoteagent', destination: '/var/lib/pulse2/xmpp_baseremoteagent/' }
   when:
     - INSTALL_TYPE != 'p'

 - name: SHARE - Change path to folders in syncthing config file
   command: xmlstarlet ed --inplace -u "configuration/folder[@id='{{ item.share_id }}']/@path" -v "{{ item.destination }}" /var/lib/syncthing/.config/syncthing/config.xml
   with_items:
     - { share_id: 'pulsemaster_packages', syncthing_folder: 'packages', destination: '/telediffusion/4/' }
     - { share_id: 'pulsemaster_downloads', syncthing_folder: 'downloads', destination: '/var/lib/pulse2/clients/' }
     - { share_id: 'pulsemaster_bootmenus', syncthing_folder: 'bootmenus', destination: '/var/lib/pulse2/imaging/bootmenus/' }
     - { share_id: 'pulsemaster_baseremoteagent', syncthing_folder: 'baseremoteagent', destination: '/var/lib/pulse2/xmpp_baseremoteagent/' }
   when:
     - INSTALL_TYPE != 'p'

 - name: SYNCTHING - Restart service after editing configuration
   systemd:
     name: syncthing@syncthing
     state: restarted
   when:
     - INSTALL_TYPE != 'p'

 - name: SYNCTHING - Pause for 5 to 300 seconds to enable syncthing start
   wait_for:
     port: 22000
     delay: 5
   when:
     - INSTALL_TYPE != 'p'

 - name: SYNCTHING - Get local device id for syncthing-depl
   command: syncthing -home=/var/lib/syncthing-depl/.config/syncthing/ -device-id
   register: localdeviceid

 - name: SYNCTHING - Set LOCAL_DEVICE_ID for syncthing-depl
   set_fact:
     LOCAL_DEVICE_ID: "{{ localdeviceid.stdout }}"

 - name: SYNCTHING - Setup local apache for syncthing-depl
   copy:
     content: |
       ProxyPass /syncthing-depl/ http://localhost:8385/
       <Location /syncthing-depl/>
           ProxyPassReverse http://localhost:8385/
           Require all granted
       </Location>
     dest: '{{ APACHE_CONF_PATH }}/syncthing-depl.conf'

 - name: SYNCTHING - Reload apache
   systemd:
     name: '{{ APACHE_DAEMON }}'
     state: reloaded

 - name: SYNCTHING - Setup pulsemain apache for relay syncthing-depl
   copy:
     content: |
       ProxyPass /syncthing-depl-{{ XMPP_DOMAIN }}/ http://{{ XMPP_DOMAIN }}/syncthing-depl/
       <Location /syncthing-depl-{{ XMPP_DOMAIN }}/>
           ProxyPassReverse http://{{ XMPP_DOMAIN }}/syncthing-depl/
           Require all granted
       </Location>
     dest: '{{ APACHE_CONF_PATH }}/syncthing-depl-{{ XMPP_DOMAIN }}.conf'
   vars:
     ansible_ssh_user: root
   delegate_to: '{{ PULSEMAIN_IP }}'
   when:
     - INSTALL_TYPE != 'p'

 - name: SYNCTHING - Reload apache on pulsemain
   systemd:
     name: '{{ APACHE_DAEMON }}'
     state: reloaded
   vars:
     ansible_ssh_user: root
   delegate_to: '{{ PULSEMAIN_IP }}'

 - name: SYNCTHING - Stop service for editing configuration
   systemd:
     name: syncthing@{{ item }}
     state: stopped
   with_items:
     - syncthing
     - syncthing-depl

 - name: SYNCTHING - Get running processes
   shell: "ps aux | grep -v grep | grep -w syncthing | awk '{print $2}'"
   register: syncthing_pid

 - name: SYNCTHING - Wait until processes are stopped
   wait_for:
     path: "/proc/{{ item }}/status"
     state: absent
   with_items: "{{ syncthing_pid.stdout_lines }}"
   ignore_errors: yes
   register: kill_pid

 - name: SYNCTHING - Kill running processes
   command: "kill -9 {{ item }}"
   with_items: "{{ kill_pid.results | select('failed') | map(attribute='item') | list }}"

 - name: SYNCTHING - Define global announce server as pulsemain on relay syncthing-depl
   command: 'xmlstarlet ed --inplace -u "configuration/options/globalAnnounceServer" -v "https://{{ PULSEMAIN_FQDN }}:8443/?id={{ DISCOSRV_DEVICE_ID }}" /var/lib/syncthing-depl/.config/syncthing/config.xml'
   when:
     - INSTALL_TYPE != 'p'

 - name: SYNCTHING - Disable use of relays
   command: 'xmlstarlet ed --inplace -u "configuration/options/relaysEnabled" -v "false" /var/lib/syncthing-depl/.config/syncthing/config.xml'

 - name: SYNCTHING - Disable use of local announce
   command: 'xmlstarlet ed --inplace -u "configuration/options/localAnnounceEnabled" -v "false" /var/lib/syncthing-depl/.config/syncthing/config.xml'

 - name: SYNCTHING - Add root user
   command: 'xmlstarlet ed --inplace -s "configuration/gui" -t elem -n user -v root /var/lib/syncthing-depl/.config/syncthing/config.xml'

 - name: SYNCTHING - Set root password to ENCRYPTED_PASS
   command: xmlstarlet ed --inplace -s "configuration/gui" -t elem -n password -v {{ ENCRYPTED_PASS }} /var/lib/syncthing-depl/.config/syncthing/config.xml

 - name: SYNCTHING - Define listen port to 23000
   command: 'xmlstarlet ed --inplace -u "configuration/options/listenAddress" -v "tcp://0.0.0.0:23000" /var/lib/syncthing-depl/.config/syncthing/config.xml'

 - name: SYNCTHING - Define gui listen port to 8385
   command: 'xmlstarlet ed --inplace -u "configuration/gui/address" -v "127.0.0.1:8385" /var/lib/syncthing-depl/.config/syncthing/config.xml'

 - name: SYNCTHING - Restart service after editing configuration
   systemd:
     name: syncthing@syncthing-depl
     state: restarted

 - name: SYNCTHING - Pause for 5 to 300 seconds to enable syncthing start
   wait_for:
     port: 23000
     delay: 5

 - name: SYNCTHING - Get api key from syncthing-depl
   command: xmlstarlet sel -t -v "configuration/gui/apikey" /var/lib/syncthing-depl/.config/syncthing/config.xml
   register: apikey

 - name: SYNCTHING - Set API_KEY
   set_fact:
     API_KEY: "{{ apikey.stdout }}"

 - name: SYNCTHING - Configure API_KEY in stman
   command: "stman configure -k {{ API_KEY }} -p 8385 -n depl"

 - name: SYNCTHING - Define XMPP_DOMAIN for device name
   command: "stman -d depl device edit -n {{ XMPP_DOMAIN }} {{ LOCAL_DEVICE_ID }}"

 - name: SYNCTHING - Add relayserver device as introducer on pulsemain syncthing-depl
   command: 'stman -d depl device add --name {{ XMPP_DOMAIN }} --address tcp://{{ IP_ADDRESS }}:23000 {{ LOCAL_DEVICE_ID }} --introducer'
   vars:
     ansible_ssh_user: root
   delegate_to: '{{ PULSEMAIN_IP }}'
   when:
     - INSTALL_TYPE != 'p'

 - name: SYNCTHING - Find pulsemain device id
   command: syncthing -home=/var/lib/syncthing-depl/.config/syncthing/ -device-id
   vars:
     ansible_ssh_user: root
   delegate_to: '{{ PULSEMAIN_IP }}'
   register: resid
   when:
     - INSTALL_TYPE != 'p'

 - name: SYNCTHING - Set PULSEMAIN_DEVICE_ID
   set_fact:
     PULSEMAIN_DEVICE_ID: '{{ resid.stdout }}'
   vars:
     ansible_ssh_user: root
   delegate_to: '{{ PULSEMAIN_IP }}'
   when:
     - INSTALL_TYPE != 'p'

 - name: SYNCTHING - Add pulsemain device as introducer on relay syncthing-depl
   command: 'stman -d depl device add --name pulse --address tcp://{{ PULSEMAIN_IP }}:23000 --introducer {{ PULSEMAIN_DEVICE_ID }}'
   when:
     - INSTALL_TYPE != 'p'

 - name: SYNCTHING - Stop service for editing configuration
   systemd:
     name: syncthing@{{ item }}
     state: stopped
   with_items:
     - syncthing
     - syncthing-depl
   when:
     - INSTALL_TYPE != 'p'

 - name: SYNCTHING - Get running processes
   shell: "ps aux | grep -v grep | grep -w syncthing | awk '{print $2}'"
   register: syncthing_pid
   when:
     - INSTALL_TYPE != 'p'

 - name: SYNCTHING - Wait until processes are stopped
   wait_for:
     path: "/proc/{{ item }}/status"
     state: absent
   with_items: "{{ syncthing_pid.stdout_lines }}"
   ignore_errors: yes
   register: kill_pid
   when:
     - INSTALL_TYPE != 'p'

 - name: SYNCTHING - Kill running processes
   command: "kill -9 {{ item }}"
   with_items: "{{ kill_pid.results | select('failed') | map(attribute='item') | list }}"
   when:
     - INSTALL_TYPE != 'p'

 - name: SYNCTHING - Auto-accept shares coming from pulsemain
   command: xmlstarlet ed --inplace -u "configuration/device[@id='{{ PULSEMAIN_DEVICE_ID }}']/autoAcceptFolders" -v true /var/lib/syncthing-depl/.config/syncthing/config.xml
   when:
     - INSTALL_TYPE != 'p'

 - name: SYNCTHING - Restart service after editing configuration
   systemd:
     name: syncthing@syncthing-depl
     state: restarted
   when:
     - INSTALL_TYPE != 'p'

 - name: SYNCTHING - Pause for 5 to 300 seconds to enable syncthing start
   wait_for:
     port: 23000
     delay: 5
   when:
     - INSTALL_TYPE != 'p'

 - name: SYNCTHING - Delete the default share
   command: 'stman -d depl folder remove "Default Folder"'

 - name: SYNCTHING - Update relay config with apikey and device id
   command: crudini --set /etc/pulse-xmpp-agent/relayconf.ini.local syncthing-deploy {{ item.parameter }} {{ item.value }}
   with_items:
     - { parameter: 'syncthing_gui_apikey', value: '"{{ API_KEY }}"'}
     - { parameter: 'syncthing_device_id', value: '"{{ LOCAL_DEVICE_ID }}"'}
   no_log: True

 - name: SYNCTHING - Restart syncthing services
   systemd:
     name: syncthing@{{ item }}
     state: restarted
   with_items:
     - syncthing
     - syncthing-depl
   when:
     - INSTALL_TYPE != 'p'

##########################################################
#                   SSH server setup                     #
##########################################################

 - name: SSH - Setup alternate SSH port if defined
   lineinfile:
     dest: /etc/ssh/sshd_config
     regexp: "{{ item.regex }}"
     line: "{{ item.line }}"
     insertafter: "{{ item.insertafter }}"
   with_items:
     - { regex: '^#?\s*Port 22$', line: 'Port 22', insertafter: '' }
     - { regex: "^Port {{ REVERSE_SSH_PORT }}$", line: "Port {{ REVERSE_SSH_PORT }}", insertafter: 'Port 22' }
     - { regex: '^#?\s*GatewayPorts .*$', line: 'GatewayPorts yes', insertafter: '' }
   when:
     - REVERSE_SSH_PORT != ''

 - name: SSH - Setup GatewayPorts
   lineinfile:
     dest: /etc/ssh/sshd_config
     regexp: '^#?\s*GatewayPorts .*$'
     line: 'GatewayPorts yes'

 - name: SSH - Restart SSH server
   service:
     name: sshd
     state: restarted

##########################################################
#                Main Pulse server setup                 #
##########################################################

 - name: MAINPULSE - Active pulse module
   command: a2enconf pulse
   when:
     - ansible_distribution == 'Debian'
     - INSTALL_TYPE == 'p'

 - name: MAINPULSE - Reload apache on Debian
   systemd:
     name: '{{ APACHE_DAEMON }}'
     state: reloaded
   when:
     - ansible_distribution == 'Debian'
     - INSTALL_TYPE == 'p'

##########################################################
#                  Relay server setup                    #
##########################################################

 - name: RELAY - Get current plugins list from pulsemain
   command: crudini --get /etc/pulse-xmpp-agent/relayconf.ini.local plugin pluginlist
   vars:
     ansible_ssh_user: root
   delegate_to: '{{ PULSEMAIN_IP }}'
   register: plugin_list
   when:
     - INSTALL_TYPE != 'p'

 - name: RELAY - Get connection password from pulsemain
   shell: 'crudini --get /etc/pulse-xmpp-agent/relayconf.ini.local connection password'
   vars:
     ansible_ssh_user: root
   delegate_to: '{{ PULSEMAIN_IP }}'
   register: connection_pass
   when:
     - INSTALL_TYPE != 'p'

 - name: RELAY - Register master account
   command: "ejabberdctl register master {{ XMPP_DOMAIN }} {{ connection_pass.stdout }}"
   when:
     - INSTALL_TYPE != 'p'

 - name: RELAY - Configure relay server
   command: crudini --set /etc/pulse-xmpp-agent/relayconf.ini.local {{ item.section }} {{ item.parameter }} {{ item.value }}
   with_items:
     - { section: 'plugin', parameter: 'pluginlist', value: '"{{ plugin_list.stdout }}"'}
     - { section: 'connection', parameter: 'password', value: '"{{ connection_pass.stdout }}"'}
     - { section: 'connection', parameter: 'server', value: '{{ IP_ADDRESS }}'}
     - { section: 'type', parameter: 'guacamole_baseurl', value: '"http://{{ PULSEMAIN_FQDN }}/guacamole-{{ XMPP_DOMAIN }}/#/client/@@CUX_ID@@?username=root&password={{ GUACAMOLE_ROOT_PASSWORD }}"'}
     - { section: 'chat', parameter: 'domain', value: '{{ XMPP_DOMAIN }}'}
     - { section: 'jid_01', parameter: 'jidname', value: '"RS{{ XMPP_DOMAIN }}@{{ XMPP_DOMAIN }}/pulse01a"'}
     - { section: 'global', parameter: 'allow_order', value: 'master_reg'}
     - { section: 'type', parameter: 'geolocalisation', value: 'False'}
   when:
     - INSTALL_TYPE != 'p'
   no_log: True

 - name: RELAY - Configure relay server public IP if defined
   command: crudini --set /etc/pulse-xmpp-agent/relayconf.ini.local type {{ item.parameter }} {{ item.value }}
   with_items:
     - { parameter: 'request_type', value: 'public'}
     - { parameter: 'public_ip', value: '{{ PUBLIC_IP }}'}
   when:
     - PUBLIC_IP != ''
     - INSTALL_TYPE != 'p'

 - name: RELAY - Add gucamoleconf to list of plugins
   command: "crudini --set --list /etc/pulse-xmpp-agent/relayconf.ini.local plugin pluginlist guacamoleconf"

 - name: RELAY - Configure guacamoleconf plugin
   command: "crudini --set /etc/pulse-xmpp-agent/guacamoleconf.ini.local parameters guacamole_dbpasswd {{ ROOT_PASSWORD }}"

 - name: RELAY - Fetch plugin ini files from pulsemain
   fetch:
     src: "/etc/pulse-xmpp-agent/{{ item }}.ini.local"
     dest: "/tmp/{{ ansible_hostname }}-{{ item }}.ini.local"
     flat: yes
   with_items:
     - applicationdeploymentjson
     - downloadfileexpert
     - downloadfile
     - guacamole
     - reverse_ssh_on
     - manage_scheduler_relay
   vars:
     ansible_ssh_user: root
   delegate_to: '{{ PULSEMAIN_IP }}'
   ignore_errors: yes
   when:
     - INSTALL_TYPE != 'p'

 - name: RELAY - Copy plugin ini files to relay
   copy:
     src: "/tmp/{{ ansible_hostname }}-{{ item }}.ini.local"
     dest: "/etc/pulse-xmpp-agent/{{ item }}.ini.local"
     owner: root
     group: root
     mode: '644'
   with_items:
     - applicationdeploymentjson
     - downloadfileexpert
     - downloadfile
     - guacamole
     - reverse_ssh_on
     - manage_scheduler_relay
   ignore_errors: yes
   when:
     - INSTALL_TYPE != 'p'

 - name: RELAY - Enable package watching service
   systemd:
     enabled: yes
     state: restarted
     name: pulse-package-watching
   when:
     - INSTALL_TYPE != 'p'

 - name: RELAY - Delete lines from /etc/hosts
   lineinfile:
     dest: "/etc/hosts"
     regexp: "{{ item }}"
     state: absent
   with_items:
     - '^.* pulse$'
     - '^.* {{ XMPP_DOMAIN }}$'
   when:
     - INSTALL_TYPE != 'p'

 - name: RELAY - Update /etc/hosts
   lineinfile:
     line: "{{ item.line }}"
     dest: "/etc/hosts"
     regexp: "{{ item.regex }}"
   with_items:
     - { regex: '^.* pulse$', line: '{{ PULSEMAIN_IP }} pulse' }
     - { regex: '^.* {{ XMPP_DOMAIN }}$', line: '{{ IP_ADDRESS }} {{ XMPP_DOMAIN }}' }
   when:
     - INSTALL_TYPE != 'p'

 - name: RELAY - Update /etc/hosts on pulsemain
   lineinfile:
     line: '{{ IP_REMOTE_SERVER }} {{ XMPP_DOMAIN }}'
     dest: /etc/hosts
     regexp: '^.* {{ XMPP_DOMAIN }}$'
   vars:
     ansible_ssh_user: root
   delegate_to: '{{ PULSEMAIN_IP }}'
   when:
     - INSTALL_TYPE != 'p'

 - name: RELAY - Enable service
   systemd:
     enabled: yes
     state: restarted
     name: pulse-xmpp-agent-relay
   when:
     - INSTALL_TYPE != 'p'

 - name: RELAY - Pause for 2 minutes to start and register the relay
   pause:
     minutes: 2

 - name: RELAY - Get cluster id if it exists
   command: "mysql -h {{ DBHOST }} -P {{ DBPORT }} -u{{ DBADMINUSER }} -p{{ DBADMINPASSWD }} -s xmppmaster -e \"SELECT id from cluster_ars where name='{{ CLUSTER_NAME }}'\""
   register: existingclusterid
   vars:
     ansible_ssh_user: root
   delegate_to: '{{ PULSEMAIN_IP }}'
   when:
     - INSTALL_TYPE != 'p'

 - name: RELAY - Set CLUSTER_ID if cluster already exists
   set_fact:
     CLUSTER_ID: "{{ existingclusterid.stdout }}"
   when:
     - INSTALL_TYPE != 'p'
     - existingclusterid.stdout != ''

 - name: RELAY - Get MAX_ID FROM cluster_ars when cluster does not exist
   command: 'mysql -h {{ DBHOST }} -P {{ DBPORT }} -u{{ DBADMINUSER }} -p{{ DBADMINPASSWD }} -s xmppmaster -e "SELECT MAX(id) FROM cluster_ars"'
   register: maxid
   vars:
     ansible_ssh_user: root
   delegate_to: '{{ PULSEMAIN_IP }}'
   when:
     - INSTALL_TYPE != 'p'
     - existingclusterid.stdout == ''

 - name: RELAY - Set CLUSTER_ID
   set_fact:
     CLUSTER_ID: "{{ maxid.stdout | int + 1 }}"
   when:
     - INSTALL_TYPE != 'p'
     - existingclusterid.stdout == ''

 - name: RELAY - Create cluster if it does not exist
   command: "mysql -h {{ DBHOST }} -P {{ DBPORT }} -u{{ DBADMINUSER }} -p{{ DBADMINPASSWD }} -s xmppmaster -e \"INSERT INTO cluster_ars (id,name,description) VALUES ({{ CLUSTER_ID }},'{{ CLUSTER_NAME }}','{{ CLUSTER_NAME }}')\""
   vars:
     ansible_ssh_user: root
   delegate_to: '{{ PULSEMAIN_IP }}'
   when:
     - existingclusterid.stdout == ''

 - name: RELAY - Get MAX_ID FROM has_cluster_ars
   command: 'mysql -h {{ DBHOST }} -P {{ DBPORT }} -u{{ DBADMINUSER }} -p{{ DBADMINPASSWD }} -s xmppmaster -e "SELECT MAX(id) FROM has_cluster_ars"'
   register: maxid
   vars:
     ansible_ssh_user: root
   delegate_to: '{{ PULSEMAIN_IP }}'
   when:
     - INSTALL_TYPE != 'p'

 - name: RELAY - Set NEXT_ID
   set_fact:
     NEXT_ID: "{{ maxid.stdout | int + 1 }}"
   when:
     - INSTALL_TYPE != 'p'

 - name: RELAY - Get RELAYSERVER_ID FROM relayserver
   command: "mysql -h {{ DBHOST }} -P {{ DBPORT }} -u{{ DBADMINUSER }} -p{{ DBADMINPASSWD }} -s xmppmaster -e \"SELECT id FROM relayserver WHERE nameserver='{{ XMPP_DOMAIN }}'\""
   register: relayserverid
   vars:
     ansible_ssh_user: root
   delegate_to: '{{ PULSEMAIN_IP }}'
   when:
     - INSTALL_TYPE != 'p'

 - name: RELAY - Set RELAYSERVER_ID
   set_fact:
     RELAYSERVER_ID: "{{ relayserverid.stdout }}"
   when:
     - INSTALL_TYPE != 'p'

 - name: RELAY - Relay check if id_ars is already inserted
   command: "mysql -h {{ DBHOST }} -P {{ DBPORT }} -u{{ DBADMINUSER }} -p{{ DBADMINPASSWD }} xmppmaster -Ns -e \"SELECT COUNT(has_cluster_ars.id_ars) as nb_ars from has_cluster_ars where has_cluster_ars.id_ars = '{{ RELAYSERVER_ID }}'\""
   vars:
     ansible_ssh_user: root
   delegate_to: '{{ PULSEMAIN_IP }}'
   register: relay_is_inserted

 - name: RELAY - Insert relay in its cluster
   command: "mysql -h {{ DBHOST }} -P {{ DBPORT }} -u{{ DBADMINUSER }} -p{{ DBADMINPASSWD }} xmppmaster -e \"INSERT INTO has_cluster_ars (id,id_ars,id_cluster) VALUES ('{{ NEXT_ID }}','{{ RELAYSERVER_ID }}','{{ CLUSTER_ID }}')\""
   vars:
     ansible_ssh_user: root
   delegate_to: '{{ PULSEMAIN_IP }}'
   when:
     - relay_is_inserted.stdout == "0"

 - name: RELAY - Insert relay in its cluster
   command: "mysql -h {{ DBHOST }} -P {{ DBPORT }} -u{{ DBADMINUSER }} -p{{ DBADMINPASSWD }} xmppmaster -e \"UPDATE has_cluster_ars SET id_cluster = '{{ CLUSTER_ID }}' WHERE id_ars = '{{ RELAYSERVER_ID }}'\""
   vars:
     ansible_ssh_user: root
   delegate_to: '{{ PULSEMAIN_IP }}'
   when:
     - relay_is_inserted.stdout != "0"

 - name: RELAY - Create substituteconf records
   command: "mysql -h {{ DBHOST }} -P {{ DBPORT }} -u{{ DBADMINUSER }} -p{{ DBADMINPASSWD }} xmppmaster -e \"INSERT INTO xmppmaster.substituteconf (type,jidsubtitute,relayserver_id) VALUES ('{{ item.type }}','{{ item.jid }}','{{ RELAYSERVER_ID }}')\""
   with_items:
     - { type: 'assessor', jid: 'master_asse@pulse' }
     - { type: 'subscription', jid: 'master_subs@pulse' }
     - { type: 'registration', jid: 'master_reg@pulse' }
     - { type: 'inventory', jid: 'master_inv@pulse' }
     - { type: 'logger', jid: 'master_log@pulse' }
   vars:
     ansible_ssh_user: root
   delegate_to: '{{ PULSEMAIN_IP }}'
   when:
     - INSTALL_TYPE != 'p'

 - name: RELAY - Enable autoupdatebyrelay on pulsemain
   command: "{{ item }}"
   with_items:
     - 'crudini --set /etc/pulse-xmpp-agent-substitute/loadautoupdate.ini parameters autoupdatebyrelay True'
     - 'crudini --set /etc/mmc/plugins/xmppmaster.ini.local global autoupdatebyrelay True'
   vars:
     ansible_ssh_user: root
   delegate_to: '{{ PULSEMAIN_IP }}'
   when:
     - INSTALL_TYPE != 'p'

 - name: RELAY - Enable publishing of agent updates on relay
   copy:
     content: |
        Alias /downloads /var/lib/pulse2/clients/
        <Directory /var/lib/pulse2/clients/>
          Options +Indexes
          IndexOptions FancyIndexing VersionSort SuppressDescription FoldersFirst XHTML HTMLTable NameWidth=* IgnoreCase SuppressRules IconHeight=16 IconWidth=16
          IndexOrderDefault Descending Date
          IndexIgnore agent config templates vpn *.conf *.in generate* create-repos.sh artwork libs *.nsi *.sfx *.7z *.ini *.xml .??* *~ *# *.css *.html *.plist rbash runpulseagent *.buildinfo *.dsc *.changes pulse-agent-linux package Pulse2AgentsInstaller.tar pulse-service.* create-profile.ps1
          AddIcon /downloads/win/artwork/install.ico Pulse-Agent-windows-FULL-latest.exe Pulse-Agent-windows-MINIMAL-latest.exe Pulse-Agent-mac-MINIMAL-latest.pkg.tar.gz Pulse-Agent-linux-MINIMAL-latest.sh
          HeaderName "/downloads/HEADER.html"
          IndexStyleSheet "/downloads/style.css"
          AllowOverride None
          Require all granted
        </Directory>
     dest: '{{ APACHE_CONF_PATH }}/pulse.conf'
   when:
     - INSTALL_TYPE != "p"

 - name: RELAY - Reload apache
   systemd:
     name: '{{ APACHE_DAEMON }}'
     state: reloaded
   when:
     - INSTALL_TYPE != "p"

 - name: RELAY - Add relay root public key to pulsetransfert authorized keys on pulsemain
   authorized_key:
     user: root
     state: present
     key: "{{ sshkey.ssh_public_key }}"
     path: /var/lib/pulse2/file-transfer/.ssh/authorized_keys
   vars:
     ansible_ssh_user: root
   delegate_to: '{{ PULSEMAIN_IP }}'
   when:
     - INSTALL_TYPE != 'p'

 - name: RELAY - Disable Quick deploy
   blockinfile:
     dest: /etc/pulse-xmpp-agent/relayconf.ini.local
     block: |
       [quick_deploy]
       max_size_stanza_xmpp = 0
     state: present

##########################################################
#                Certificate Generation                  #
##########################################################

 - name: CERTS - Remove relay from certificat trust, we will redo it.
   lineinfile:
     dest: /var/lib/pulse2/pki/index.txt
     state: absent
     regexp: '{{ SERVER_FQDN }}$'
   vars:
     ansible_ssh_user: root
   delegate_to: '{{ PULSEMAIN_IP }}'
   when:
     - INSTALL_TYPE != 'p'

 - name: CERTS - Create the certificates on pulsemain
   shell: "{{ item }}"
   with_items:
     - 'crudini --set /var/lib/pulse2/pki/conf/pulse.cnf alt_names DNS.1 {{ SERVER_FQDN }}'
     - 'crudini --set /var/lib/pulse2/pki/conf/pulse.cnf alt_names DNS.2 {{ XMPP_DOMAIN }}'
     - 'openssl req -config /var/lib/pulse2/pki/conf/pulse.cnf -subj "/countryName=FR/organizationName=SIVEO/commonName={{ SERVER_FQDN }}" -passout pass:{{ PKI_PASSWORD }} -batch -extensions server_cert -new -keyout /var/lib/pulse2/pki/private/{{ SERVER_FQDN }}.key.pem -out /var/lib/pulse2/pki/req/{{ SERVER_FQDN }}.req.pem'
     - 'openssl ca -config /var/lib/pulse2/pki/conf/pulse.cnf -name CA_Intermediate -passin pass:{{ PKI_PASSWORD }} -batch -extensions server_cert -extfile /var/lib/pulse2/pki/conf/pulse.cnf -keyfile /var/lib/pulse2/pki/private/cakey.pem -out /var/lib/pulse2/pki/newcerts/{{ SERVER_FQDN }}.cert.pem -infiles /var/lib/pulse2/pki/req/{{ SERVER_FQDN }}.req.pem'
     - 'openssl rsa -passin pass:{{ PKI_PASSWORD }} -in /var/lib/pulse2/pki/private/{{ SERVER_FQDN }}.key.pem -out /var/lib/pulse2/pki/{{ SERVER_FQDN }}.pem'
     - 'cat /var/lib/pulse2/pki/newcerts/{{ SERVER_FQDN }}.cert.pem >> /var/lib/pulse2/pki/{{ SERVER_FQDN }}.pem'
     - 'crudini --del /var/lib/pulse2/pki/conf/pulse.cnf alt_names'
   vars:
     ansible_ssh_user: root
   delegate_to: '{{ PULSEMAIN_IP }}'
   when:
     - INSTALL_TYPE != 'p'
   no_log: True

 - name: CERTS - Set proper permissions on the certificates on pulsemain
   file:
     path: "{{ item.path }}"
     mode: "{{ item.mode }}"
     state: "{{ item.state }}"
   with_items:
     - { path: '/var/lib/pulse2/pki/private/{{ SERVER_FQDN }}.key.pem', mode: '0600', state: 'file' }
     - { path: '/var/lib/pulse2/pki/newcerts/{{ SERVER_FQDN }}.cert.pem', mode: '0644', state: 'file' }
     - { path: '/var/lib/pulse2/pki/{{ SERVER_FQDN }}.pem', mode: '0644', state: 'file' }
     - { path: '/var/lib/pulse2/pki/req/{{ SERVER_FQDN }}.req.pem', mode: '', state: 'absent' }
   vars:
     ansible_ssh_user: root
   delegate_to: '{{ PULSEMAIN_IP }}'

 - name: CERTS - Fetch relay certificate from pulsemain
   fetch:
     src: "/var/lib/pulse2/pki/{{ SERVER_FQDN }}.pem"
     dest: "/tmp/{{ ansible_hostname }}-temp.pem"
     flat: yes
   vars:
     ansible_ssh_user: root
   delegate_to: '{{ PULSEMAIN_IP }}'
   when:
     - INSTALL_TYPE != 'p'

 - name: CERTS - Copy relay certificate to /etc/ejabberd/{{ XMPP_DOMAIN }}.pem
   copy:
     src: "/tmp/{{ ansible_hostname }}-temp.pem"
     dest: "/etc/ejabberd/{{ XMPP_DOMAIN }}.pem"
   when:
     - INSTALL_TYPE != 'p'

 - name: CERTS - Replace ejabberd.pem by the certificate
   lineinfile:
     dest: /etc/ejabberd/ejabberd.yml
     regexp: '^\s*certfile: .*$'
     line: '    certfile: "/etc/ejabberd/ejabberd.pem"'
   when:
     - INSTALL_TYPE != 'p'

 - name: CERTS - Restart service
   systemd:
     name: ejabberd
     state: restarted

##########################################################
#                   Specifique CNAM                      #
##########################################################

 - name: CNAM - crudini quick_deploy max_size_stanza_xmpp 0
   shell: crudini --set /etc/pulse-xmpp-agent/relayconf.ini.local quick_deploy max_size_stanza_xmpp 0

##########################################################
#                     Final steps                        #
##########################################################

 - name: FINAL - Get api key from syncthing on pulsemain
   command: xmlstarlet sel -t -v "configuration/gui/apikey" /var/lib/syncthing/.config/syncthing/config.xml
   register: apikey
   vars:
     ansible_ssh_user: root
   delegate_to: '{{ PULSEMAIN_IP }}'
   when:
     - INSTALL_TYPE != 'p'

 - name: FINAL - Force synch of syncthing folders from pulsemain
   command: 'curl -X POST -H "X-API-Key: {{ apikey.stdout }}" http://localhost:8384/rest/db/override?folder={{ item }}'
   with_items:
     - pulsemaster_packages
     - pulsemaster_baseremoteagent
     - pulsemaster_bootmenus
     - pulsemaster_downloads
   vars:
     ansible_ssh_user: root
   delegate_to: '{{ PULSEMAIN_IP }}'
   when:
     - INSTALL_TYPE != 'p'

 - name: FINAL - Pause for 30 seconds to finalize configs
   pause:
     seconds: 30

 - name: FINAL - Restart pulse relay service
   systemd:
     name: pulse-xmpp-agent-relay
     state: restarted
   when:
     - INSTALL_TYPE != 'p'
